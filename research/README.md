# research

Pointers to the research artifacts behind Columna:

- **Atlas** — (DOI: TODO) the coordinate/anchor foundation.
- **Two-Anchors** — (DOI: TODO) the V/M/B anchor model.
- **Benchmark** — the 299,934-transaction warehouse used for the real-data proofs; a schema-
  identical mini-warehouse (≤ 2 MB) ships under
  `packages/columna-core/tests/fixtures/mini_warehouse` for CI, generated by
  `packages/columna-core/tests/fixtures/make_fixture.py`.

(DOIs to be filled in when the papers are published; this file is a placeholder index in WP-0.)
