"""Columna Platform — the successor execution path. PROOF STAGE, NOT PUBLISHED.

Proof A: a governed family identity and its standing survive the passage into retained material
state and back out through serving, without the physical carrier redefining the meaning — and each
neighbouring unlawful case fails for the RIGHT REASON, in the RIGHT JURISDICTION.

Two findings stopped the path short of the wire. Both are recorded in `serving.py`'s module
docstring and pinned by tests in `tests/test_proof_a_findings.py`; neither was worked around.
"""
__version__ = "0.0.0"
