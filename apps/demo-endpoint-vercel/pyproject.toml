[project]
name = "columna-demo-endpoint"
version = "0.1.0"
description = "RETIRED surface (P1-32, 2026-09-02) — answers 410 Gone; serves no analytical result."
requires-python = ">=3.12"
# No dependencies, and now no data: the generator, the vendored parser and the committed capture
# were deleted at retirement. index.py is stdlib-only and returns 410 for every request.
dependencies = []

[tool.vercel]
entrypoint = "index:handler"
