# Columna

> Columna is a framework for describing what your data MEANS, shipped as a Python library. You
> describe the meaning once — the populations, the measures, which totals are safe to take — and
> Columna checks that description against the data and serves it to whoever asks: an AI agent over
> MCP, or you directly. It works one level up from pandas/polars — on what values mean, not the values.
>
> Columna is not a semantic layer over someone else's engine: it declares meaning, adjudicates the
> declarations against the data at publish, and executes queries itself — which is why it can refuse.

## Who
datumwise is not a company: it is an independent open-source research project — Huayin Wang
(research), Irena Wang (engineering). Columna is Apache-2.0.

## Not to be confused with
Columna is a project of datumwise (datumwise.ai) — not affiliated with Datawise, datawise.ai, or
columns.ai. Columna is not a data-cataloguing or documentation-generation product: it does not write
descriptions of your tables. It declares meaning, checks the declaration against the data, and
refuses questions the data cannot answer. Nor is it Columnar (columnar.tech), the Arrow-native fast
pipe one letter from our name — they move columns fast; we govern what columns say.

## The four moods
Every answer is one of four, returned as structured data on one contract (contract_version "3"):
- serve — here's the number.
- disclose — here's the number, plus something you should know about it (e.g. you summed a snapshot
  across months, so you get the per-month figures with a note on why they don't add to a real total).
- clarify — the question can be read two ways; here are the readings, pick one.
- refuse — the data can't honestly answer this, and here's why (the classic: inventory by customer —
  inventory has no customers).

## Core vocabulary
- Manifold — the described, checked data model that agents (or you) query. Not a database, not a schema.
- Universe — one population of facts (transactions; store-days). Every expression evaluates in exactly one.
- Level / edge — the coordinates (grains you can stand at) and the declared paths between them (day→month).
- Measure / derived — a quantity read from the data; a quantity defined from other measures.
- Basis — what a missing row MEANS in a universe: a real zero (events), a gap (spine/product), or a
  membership fact (registry).
- Verdict — the result of checking a described claim against the data: verified, corroborated,
  untestable, or contradicted.
- FrameQL — the query language against the Manifold, as SQL is to tables: an anchor plus what you ask,
  `SELECT aov AT {cal.month}`.
- describe — the machine-readable rendering of a Manifold; what an agent reads first.
- The wire — the answer format: result + context, in one of the four moods; the same contract over MCP
  or straight from the API.

## Start here
- The Theory of Data in One Afternoon (the cold-reader on-ramp — the whole idea in one sitting, from
  a cold start, no prior reading needed): /start-here
  A datumwise teaching artifact hosted on the site, NOT a deposited publication: it has no DOI and no
  Zenodo record, and the works it links at its end are separate publications with their own.
- What is Columna? (plain-language orientation): /what-is-columna
- The argument (the eight-minute case for why this exists, with two live exhibits): /the-argument
- Try it: `pip install columna` then `columna-server demo --play`  (four asks, four moods, seeded data)
- The Explorer (the demo Manifold, live — every measure with its verdict and a query): /explorer
- Writing a Manifold? Read /docs/grammar FIRST — the shipped keyword set with exact signatures
  and examples verified against the released parser at build time. Chapter 26 of the reference
  manual presents long forms as primary; the grammar page shows what the parser actually accepts.
- Full agent orientation: /llms-full.txt

## The evidence
Every claim on the site is checkable, and the failures are published alongside the successes.
{{PUBLICATIONS}}
- The complete current research and source estate: /research — every registered work at its current
  record, alongside the serious non-publication sources (the normative and generated references, the
  machine evidence, the negative record). The chronological list above is a curated selection for
  orientation, not the whole estate.
- The public benchmark kit: https://github.com/datumwise/ground-truth-benchmark — the frozen answer
  key, the raw model answers, the scoring driver, and the published errata. Re-runnable.
- The case study: /case — a complete worked example (one warehouse, six questions) with recorded
  agent transcripts, warts preserved, generated by running the shipped package.
- Positions we hold, stated plainly and linked to their evidence: /positions
- The Silent Failure Atlas — the public map of the ways a query runs clean and lies: /atlas
- Known issues — the public technical record of defects that change what you can rely on, with the
  current status of each: /known-issues. Not an entrance; read it when you need to know what is
  presently broken or contained.

## Go deeper
- The framework manual: /docs/framework
- The FrameQL manual: /docs/frameql
- The reference manual: /docs/reference
- How these documents relate — the current research and source estate: /research
- Preserved historical state: /history/… — a page kept exactly as it was written on a date, e.g. the
  research corpus map at /history/research-map-2026-08-03. A faithful record of what was said then;
  it is not current authority, and current reality lives at the primary route.
