---
// /positions/practice-needs-a-theory — a foundations position, ratified. Renders VERBATIM
// (byte-identical to the ratified attachment; see the corpus copy). The file's own byline stamps its
// date ("A position from datumwise · 2026-07-30"); the page renders no second stamp.
//
// FOUNDATIONS piece, not a product piece: the P/R figure is deliberately absent (Huayin, 2026-07-30),
// so this uses the plain <Content /> render — no figure seam, no evidence footer re-authored here (the
// body carries its own DOI links).
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Content } from '../../content/corpus/position_practice_needs_a_theory_v1_1.md';
---
<BaseLayout title="Practice Needs a Firmer Foundation — datumwise"
  description="A position held by datumwise: tooling cannot fix a foundation problem — recurring, nameable failure shapes are the symptom of a foundational defect, and the theory of data is now written down.">
  <div class="page-head">
    <p class="kicker"><a href="/positions">Positions</a></p>
  </div>

  <article class="prose position">
    <Content />
  </article>
</BaseLayout>

<style>
  .page-head { width: min(100% - 3rem, var(--measure)); margin: 0 auto; }
  .kicker a { color: var(--ink-faint); text-decoration: none; }
  .kicker a:hover { color: var(--ink-soft); }
</style>
