---
// /positions/the-two-great-sources-of-silent-analytical-failure — the site edition of the paper of
// record (doi:10.5281/zenodo.21553379), ratified v1.1. Renders VERBATIM (byte-identical to the
// ratified attachment); the file's own byline stamps its date + version and names the Zenodo paper,
// and its evidence footer (five links + the benchmark kit + /case) is pre-wired in the file.
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Content } from '../../content/corpus/position_two_great_sources_site_v1_1.md';
---
<BaseLayout title="The Two Great Sources of Silent Analytical Failure — datumwise"
  description="A position held by datumwise: silent analytical failure has structure — four degrees of freedom, two of them structurally deep — so the defense must be architectural in exactly two places.">
  <div class="page-head">
    <p class="kicker"><a href="/positions">Positions</a></p>
  </div>

  <article class="prose position">
    <Content />
  </article>
</BaseLayout>

<style>
  .page-head { width: min(100% - 3rem, var(--measure)); margin: 0 auto; }
  .kicker a { color: var(--ink-faint); text-decoration: none; }
  .kicker a:hover { color: var(--ink-soft); }
</style>
