---
// /positions/never-let-your-agent-touch-the-database — measure migration (2026-08-03, pack #4 B.2.4):
// the page source is SWAPPED to the delivered v1.1 paper edition (byte-consistent with the Zenodo
// record), replacing the earlier distinct site essay — the canonical replacement, not a sweep. The
// paper carries its own title + byline (Version 1.1 · 3 August 2026).
//
// NOTE: the prior site essay's precision/recall 2×2 figure (PrecisionRecallFigure) was seated on the
// essay's "…no. Measure it." section, which the paper edition does not contain — so the figure is
// dropped with the swap, as B.2.4 intends ("do not sweep old text that has a canonical replacement").
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Content } from '../../content/corpus/position_never_let_your_agent_v1_1.md';
const DOI = '10.5281/zenodo.21774490';
---
<BaseLayout title="Never Let Your Agent Touch the Database — datumwise"
  description="A position held by datumwise: breach, bill, and fabrication are one architectural mistake, and they retire together the moment the model stops touching the database.">
  <div class="page-head">
    <p class="kicker"><a href="/positions">Positions</a></p>
    <p class="doi mono">Paper of record — Version 1.1: <a href={`https://doi.org/${DOI}`} rel="noopener" target="_blank">doi.org/{DOI}</a></p>
  </div>

  <article class="prose position">
    <Content />
  </article>

  <p class="evidence-note">
    <strong>Note, 2026-08-03.</strong> The theory's vocabulary moved again, and this time the move
    retired a word. <em>The Theory of Data</em>, Version 4.0
    (<a href="https://doi.org/10.5281/zenodo.21774032" rel="noopener" target="_blank">10.5281/zenodo.21774032</a>)
    replaces <em>atom</em> with <strong>measure</strong> — the governed family — because the old word
    named smallestness while the object is a family with laws. A column realizes a <strong>member</strong>:
    a governed <strong>series</strong>. The position is unchanged; a family is constituted by its law,
    and so, it turns out, is a vocabulary.
  </p>

  {/* THE ONWARD MOVEMENT (Huayin, 2026-08-27, Doors §5). This piece is a Door: a stranger arrives
      because "never let your agent touch the database" makes them ask "why not?", and the page used
      to end without answering "so what governs the crossing instead?" The paper's own text is
      untouched — this is page chrome, after it, in the editorial register the evidence-note above
      already establishes. The destination is the DISCIPLINE rather than the system: the question is
      about what should govern, and Analytical Governance is the account of that; Columna is reached
      from there as its executable consequence. */}
  <p class="onward">
    <span class="onward-q">If the agent should not hold execution authority directly, what should
    govern the crossing?</span>
    <a class="onward-a" href="/analytical-governance">Analytical Governance →</a>
  </p>
</BaseLayout>

<style>
  /* The onward movement — see the note in the markup. Serif question, plain link, no button. */
  .onward { display: grid; gap: 0.4rem; margin: 2.5rem 0 0; padding-top: 1.5rem;
    border-top: 1px solid var(--rule); }
  .onward-q { font-family: var(--font-serif); font-size: 1.05rem; line-height: 1.5; color: var(--ink); }
  .onward-a { font-family: var(--font-sans); font-size: 0.92rem; color: var(--ink);
    text-decoration: underline; text-decoration-color: var(--rule-strong); width: fit-content; }
  .onward-a:hover { text-decoration-color: var(--ink-faint); }

  .page-head { width: min(100% - 3rem, var(--measure)); margin: 0 auto; }
  .kicker a { color: var(--ink-faint); text-decoration: none; }
  .kicker a:hover { color: var(--ink-soft); }
  .doi { font-size: 0.8rem; margin-top: 0.3rem; }
  .doi a { color: var(--ink-faint); }
  .evidence-note { width: min(100% - 3rem, var(--measure)); margin: 3rem auto 0; padding-top: 1.5rem;
    border-top: 1px solid var(--rule); font-size: 0.9rem; color: var(--ink-soft); }
</style>
