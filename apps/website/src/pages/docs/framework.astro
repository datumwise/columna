---
// CP-3b Tier-2 — /docs/framework. Renders the Columna framework manual (6g) from the repo-root
// docs/ residency as-is (site wiring only; the manual text is not edited here). Under the ONE-DEPLOY
// discipline this route lights up with the rest of the beat when CP-M2 lands; it is built now so it
// renders (package §2b). Editions bumped 6e → 6f (v3.1 sync) → 6g (v4.0 measure migration, 2026-08-03);
// 6e and 6f both retained as prior-edition records.
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Content } from '../../../../../docs/columna_framework_manual_6g.md';
---
<BaseLayout title="The Columna Framework manual — datumwise"
  description="The Columna framework manual (6th ed.) — the Column-Operator-Frame substrate, the grammar layer beneath the tools."
  footer="columna">
  <div class="page-head">
    <p class="kicker"><a href="/learn">Learn</a> · Manual</p>
  </div>
  <article class="prose">
    <Content />
  </article>
</BaseLayout>

<style>
  .page-head { width: min(100% - 3rem, var(--measure)); margin: 0 auto 1.5rem; }
  .kicker a { color: var(--ink-soft); }
</style>
