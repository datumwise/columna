---
// CP-3b Tier-2 — /docs/frameql. Renders the FrameQL manual (v1) from the repo-root docs/ residency
// as-is (site wiring only). Rides the CP-M2 gate + ONE-DEPLOY discipline; built now so it renders.
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Content } from '../../../../../docs/frame_ql_manual_v1.md';
---
<BaseLayout title="The FrameQL manual — datumwise"
  description="The FrameQL manual (v1) — the formal query language of Columna, completely.">
  <div class="page-head">
    <p class="kicker"><a href="/learn">Learn</a> · Manual</p>
  </div>
  <article class="prose">
    <Content />
  </article>
</BaseLayout>

<style>
  .page-head { width: min(100% - 3rem, var(--measure)); margin: 0 auto 1.5rem; }
  .kicker a { color: var(--ink-soft); }
</style>
