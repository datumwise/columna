# Open forks — decisions the code made provisionally, awaiting a ruling

Sibling to [`doctrine_gaps.md`](doctrine_gaps.md). The two are opposite directions of the same seam:

- a **doctrine gap** is *code lagging ruled doctrine* (ruled, not yet built);
- an **open fork** is *code ahead of doctrine* — the implementation had to pick something (a vocabulary
  code, a materiality, a shape) that Huayin has **not yet ruled**, so it used the closest-fitting
  existing choice and flagged it.

**The rule.** Every fork surfaced in a PR gets a row here **before that PR merges** — so nothing open
ever lives only in a PR description (prose no one is obliged to reread). A row carries the provisional
choice actually shipped, the alternatives, a recommendation, and a link to where it came from. It is
struck when Huayin rules, with the ruling and its landing named. The queue is Huayin's; this file is
only its durable form.

| # | opened | fork (the open question) | provisional choice shipped | alternatives | recommendation | source | status |
|---|---|---|---|---|---|---|---|
| ~~OF-1~~ | 2026-07-14 | **Unpinned-inline-reduction clarify reason.** Which reason code carries the engine clarify for an inline reduction with no pinned input anchor (`avg(aov)@month`)? | Reused **`ambiguous_grain`** (CLARIFY/AMBIGUOUS) — the closest fit in the closed `REASON_OUTCOME` vocabulary; no code minted. | (a) keep `ambiguous_grain` — but its gloss reads "attribute keyed at several levels", so reuse broadens it; (b) a new reserved reason `input_anchor_ambiguous`. | Reuse `ambiguous_grain` **and** widen its gloss to cover input-anchor underdetermination — unless a distinct reason aids the agent surface, in which case mint `input_anchor_ambiguous`. | [PR #18](https://github.com/datumwise/columna/pull/18); capture v0.8 §"Reduction OF a derivation"; `disclosure.py` `REASON_OUTCOME` | ~~**RULED (b)** 2026-07-14: mint `input_anchor_ambiguous` (CLARIFY/AMBIGUOUS), sibling to `co_anchor_ambiguous`; `ambiguous_grain` gloss NOT widened. Standing rule set: **one reason per contested dimension**. The clarify names the same dimension OF-2's disclosure records.~~ |
| ~~OF-2~~ | 2026-07-14 | **Pinned-inline-reduction communicative disclosure + the input_anchor-fit finding** (owed to CP-B2). Does an explicitly user-pinned input anchor (`avg(aov@day)`) owe a caveat, and if so which — material or immaterial? | Served with the **immaterial `provenance`** code (category `transport`) naming the reading; **not** the material `input_anchor` caveat. | (a) immaterial `provenance` [shipped]; (b) no disclosure at all; (c) a new reserved communicative code. | **(a).** Finding: an explicitly user-pinned anchor is a deliberate, visible choice, so it owes a *communicative* (immaterial) note — not the material `input_anchor` caveat, which is for an anchor choice imported from a name or defaulted (one the reader must weigh); an explicit pin is the reader's own. | [PR #18](https://github.com/datumwise/columna/pull/18); `disclosure_wire.py` `CATEGORY_TABLE`; CV2-2 in [`design_capture_outcome_pair_v0_1.md`](context/design_capture_outcome_pair_v0_1.md) | ~~**RULED (a)** 2026-07-14: ratified as shipped. **Boundary (durable finding):** material `input_anchor` is for an anchor choice IMPORTED from a name or DEFAULTED (one the reader must weigh); an EXPLICIT pin owes only the immaterial `provenance` note — because the wire's reader may not be the asker.~~ |

| OF-3 | 2026-07-16 | **ASSERT row-form base-row data channel.** How is a row-predicate ASSERT (`ASSERT <n> ON <u> WHERE <pred>`) data-tested at publish — the scan that checks every atom of the universe satisfies the predicate? | **UNTESTABLE** shipped: recorded on authored authority, visible in describe, never exercised. The invariant-form has its full data channel (serve LHS/RHS at the anchor, compare per op); the row-form does not yet. | (a) a bounded base-row scan through the typed connector surface (count atoms failing the predicate at the universe grain), CONTRADICTED if any violate; (b) a materialized-count assertion; (c) leave UNTESTABLE until a row-assert appears in a real manifold. | **(a)** — when a row-assert first needs teeth, through the two-ends aperture (the typed connector calls, never general SQL). **Gates nothing today; must not evaporate** (Huayin's rider, 2026-07-16). | CP-1 increment 3 ([PR #35](https://github.com/datumwise/columna/pull/35)); `adjudication.py` `_prove_assert` row branch | **OPEN** |

| OF-4 | 2026-07-16 | **Query-side `universe` arg / ON-UNIVERSE apply removal (§2c consequence).** §2c makes ON UNIVERSE dead in the query grammar (the expression law deleted its last query-side job), but the server `query(..., universe=)` arg, the wire's `on_universe`→`apply:{universe}` remediation (`disclosure_wire._wire_alternative`, `_UNIVERSE_RE`), and the agent's clarify-PICK (`agent/loop.py` applies `alts[idx].apply.universe`) still exist. | **Kept, dormant:** no §2c reason emits a universe-alternative (co_anchor was the only one, now retired), so the whole apply/pick path is dead code. The core expression/frame laws landed; the SERVER surface removal is a pinned deferral (per Huayin's scope-split ruling, OF-3 precedent). | (a) remove the arg + wire-apply + redesign the agent pick to reformulate the query (pin an input anchor) rather than apply a universe; (b) leave it dormant until a coordinated server/agent pass. | **(a)** in a coordinated server+agent increment (the agent's clarify-pick round-trip moves with it — its relay-and-never-auto-pick behavior is already §2c-correct). | CP-1 §2c ([PR #35](https://github.com/datumwise/columna/pull/35)); `columna_server/tools.py`, `server.py`, `agent/loop.py`; `disclosure_wire._wire_alternative` | **OPEN** |

| OF-5 | 2026-07-16 | **The declared spine-grid — a level's DOMAIN source.** B3 absence is only definable relative to a domain. Two customers both need it: (a) SINGLE-COLUMN events zero-fill (a lone events query has no local domain, so absence is not materialized — only the juxtaposition supplies one); (b) the spine/product COMPLETENESS oracle (internal-contiguity needs the ordered axis's min/max/distinct; boundary completeness needs the full expected grid) — a connector domain-read the typed aperture does not yet expose. | **Absence scoped to the juxtaposition** (the local domain); BASIS adjudication mints **UNTESTABLE** per type (serving follows the declaration regardless — a semantic declaration, not a shortcut). No live spine refutation channel yet. | (a) a declared spine-grid object (a domain source per orderable level) + a typed connector domain-read (min/max/distinct, membership) — unlocks single-column fill AND the spine internal-contiguity CONTRADICTED channel + registry membership; (b) leave absence juxtaposition-scoped and BASIS UNTESTABLE until Authoring declares grids. | **(a)** when the domain source is declarable (Authoring-era / the connector-aperture pass). Huayin (2026-07-16): internal-contiguity is grid-free IN PRINCIPLE (min/max/distinct on an ordered axis), but the connector domain-read it needs IS this grid object. | CP-1 increment 6 ([PR #35](https://github.com/datumwise/columna/pull/35)); `planner.run` absence pass; `adjudication._prove_basis` | **OPEN** |

| OF-6 | 2026-07-16 | **Draft persistence (cross-session serialization).** The A4 authoring loop's human turns (review → revise → declare → publish) will span sessions once a real provider / interactive `columna init` ships — at which point the in-memory `Draft` (proposals + marks + state) needs a serialization to survive between turns. | **In-memory only.** The hermetic loop runs in-process (single session), so no serialization exists; the draft state (grades, review marks, state-machine position) is lost across process boundaries. | (a) serialize the Draft (its own format, or lower-to-.cml-plus-a-review-sidecar) when the interactive loop lands; (b) keep in-memory until then. | **(a)** — its trigger is the NEXT step (wiring a real provider / interactive init is exactly when human turns start spanning sessions), so the row is opened now, not on prophecy (Huayin, 2026-07-16). | CP-2 artifact 3 ([branch `wp-cp2-init`]); `columna_core.draft`, `columna_server.init.loop` | **OPEN** |
| OF-7 | 2026-07-17 | **Package-served Explorer deployment.** The Explorer is built in `apps/website/` as a portable component (binds any describe JSON, zero site coupling). The ruled near-future path — `columna-server` offering the Explorer against a LIVE manifold's describe — is not built. | **Site-instance only.** CP-3 ships the `/explorer` demo-manifold instance; the component is portable by construction, but no package-serving entry point exists yet. | (a) a `columna-server explorer`/HTTP surface serving the component against a live manifold; (b) leave site-only until a product-deployment WP. | **(a)** as a recorded near-future path — a later WP, not CP-3 (Huayin, 2026-07-17: portable by construction now, package-served next). | CP-3 opening proposal (§a C-3, `wp_cp3_opening_proposal_v0_1.md`); capture §6 Posture | **OPEN** |
| OF-8 | 2026-07-17 | **Author-facing provenance surface (FROM/VIA).** A future Explorer layer showing the humans who DECLARED members (FROM/VIA provenance) is conceivable — but its data could NEVER come from describe (describe carries no physical/authorship identity by the §2b insulation guarantee) and would need its OWN governed source. | **Not built, not designed.** Rowed per Huayin's 'row it, don't design it' (2026-07-17): a conceivable layer, deliberately unspecified. | (a) design a governed author-provenance source + surface; (b) never build it (describe-only Explorer is complete). | **Undecided** — a file note only; revisit if/when author-provenance is a product need. Must not leak into C-3 (describe-only). | CP-3 opening proposal (§a ruling 2, `wp_cp3_opening_proposal_v0_1.md`) | **OPEN** |
| OF-9 | 2026-07-17 | **Logical attribute declarations for predicate terms.** C-2 renders universe predicates logically by DROPPING the physical table qualifier (`stores.opened_date` -> `opened_date`), so no STRUCTURAL physical identifier crosses describe — but a bare predicate attribute name still renders as the author wrote it (a residue, not a declared logical name). | **Drop-the-qualifier shipped** (CP-3 C-2): no table names, no qualified `table.column`; predicate attribute names render verbatim. The standing no-physical-identifier test asserts exactly this structural guarantee (not full verification). | (a) a definition-language extension — declared logical names for predicate attributes (a new authoring surface, with init implications); (b) leave drop-the-qualifier as the guarantee. | **(a)** as its OWN WP, NOT improvised before a freeze (Huayin, 2026-07-17). When it lands, describe renders declared names and the standing test tightens from 'no structural identifiers' to full verification. | CP-3 C-2 (`tools.py::_render_ref`); capture §2b insulation guarantee; `test_describe_insulation.py` | **OPEN** |
| OF-10 | 2026-07-17 | **§2c definition-time-population mechanism.** A DERIVED/expression that spans more than one universe is a `cross_universe` error; §2c says the population pin 'lives in definitions', but the grammar has no definition-time universe pin (only `AT <level>`). Its named customer, `sell_through_rate`, was KILLED (S-1 superseded), so the mechanism has no sponsor. | **Not built.** No definition-time population pin exists; a cross-universe derived fails closed. | (a) a definition-time population/universe pin in the DERIVED grammar (a new authoring surface); (b) leave cross-universe derived as fail-closed errors. | **Rowed, no sponsor** (Huayin, 2026-07-17): rows record designs, not sponsors — revisit if a real within-manifold need appears. | CP-3 S-1 superseded (`wp_cp3_opening_proposal_v0_1.md`); planner `_check_single_universe` | **OPEN** |

| ~~OF-16~~ | 2026-07-25 | **Reference manual §26.6 `HIERARCHY` is inverted and stale.** Its status annotation reads "single functional edge: SHIPPED 0.7.8 — via `EDGE <child> -> <parent> ALONG <lineage> VIA <table>(…)`" and "`HIERARCHY` … SCHEDULED". The truth is the exact inverse: EDGE is PURGED (case-demo §2a) and HIERARCHY is the sole shipped surface. §26.6's body also gives the signature as `HIERARCHY <name> <child> -> <parent>` — no braces, no per-hop `VIA` — which the shipped parser rejects. | **Left unchanged.** The purge sweep ([PR #90](https://github.com/datumwise/columna/pull/90)) fixed the fixture, the core README, a benchmark fixture and a test; the MANUAL is ratified content and a §26.6 rewrite is a content pass, not a fixture rewrite, so it was rowed rather than improvised. The purged-grammar CI guard carries a ROWED exemption keyed to this fork — it prints the fossil on every run and FAILS if this row is closed while the file still contains it. | (a) desk rewrites §26.6 (status annotation corrected + body signature to `HIERARCHY <lineage> { <a> -> <b> VIA t(a,b) [-> …] [; <path>] }`); (b) agent drafts the correction for ratification; (c) leave until a manuals-alignment WP. | **(a) or (b)** — the annotation is a factual SHIPPED/SCHEDULED claim that is now false in both directions, which is worse than prose drift: a reader following §26.6 writes grammar the parser rejects. Recommend it not wait for a WP. | [PR #90](https://github.com/datumwise/columna/pull/90); `scripts/check_purged_grammar.py` ROWED; `docs/columna_reference_manual_5e.md` §26.6 | ~~**CLOSED** 2026-07-26~~ · ~~Closed by the desk-drafted correction ([`specs/of16_of17_manual_correction_desk_draft_v0_1.md`](of16_of17_manual_correction_desk_draft_v0_1.md)), applied verbatim: §26.6's status annotation un-inverted (HIERARCHY SHIPPED / verification SCHEDULED) and its body signature corrected to braces + per-hop VIA, with a worked form from the shipped demo. Verified BY EXECUTION against columna-core 0.12.0: both documented forms parse, the old signature is still rejected. The purged-grammar guard's ROWED exemption is removed (the file is clean) and the guard is green.~~ |

| ~~OF-17~~ | 2026-07-25 | **Keyword-set fossils are invisible to the pattern guard.** The Chapter 26 preamble (`docs/columna_reference_manual_5e.md:1476`) lists `EDGE` in the "shipped 0.7.8 keyword set" — a purged keyword named bare, not in its full `EDGE … ALONG … VIA` surface form. `scripts/check_purged_grammar.py` deliberately requires the full shape, so it cannot see this. | **Left as-is; guard NOT widened.** Widening the pattern to catch a bare `EDGE` keyword would false-positive on every legitimate use of the word "edge" (internal taxonomy, prose, `kind="edge"`) and poison a guard with a currently-perfect record. | (a) the OF-16 docs pass, when it lands, sweeps Chapter 26's keyword set BY HAND and closes this row with it; (b) a separate narrower guard for keyword-set lines only. | **(a)** (Huayin, 2026-07-25): guards catch classes, hand-sweeps catch the rest; knowing which is which is the discipline. This row rides OF-16 to closure. | [PR #90] found the class; the preamble instance surfaced in the OF-16 draft ([`specs/of16_manual_26_6_correction_draft_v0_1.md`](of16_manual_26_6_correction_draft_v0_1.md)) | ~~**CLOSED** 2026-07-26~~ · ~~Closed WITH OF-16 per its ruled option (a) — the hand-sweep rode the docs pass. Chapter 26's preamble keyword set now reads the shipped short forms (MANIFOLD · UNIVERSE · LEVEL+ATTR · HIERARCHY · RELATE+faces · MEASURE · DERIVED · ASSERT), derived by execution from the parser's `_KW`. The stale pinned "0.7.8" is dropped in favour of "the shipped keyword set" so the sentence cannot rot at the next release. Note: the ledger link to CC's own draft was DANGLING — that file existed only in an unpushed working tree (0 commits touched it); the desk draft supersedes it and this row now points at the artifact that shipped.~~ |


| OF-18 | 2026-07-26 | **Chapter 26 status-mark audit.** §26.6's SHIPPED/SCHEDULED marks were inverted; nothing has checked whether the chapter's OTHER subsections (`DIMENSION`, `ALIAS`, `ASSERT`, `WITHHOLD`, §26.10) carry stale marks too. A status annotation is a factual claim about what ships, and one was wrong by 180°. | **Not audited.** OF-16 deliberately touched only §26.6 + the preamble; a chapter-wide audit was rowed rather than smuggled into a one-section correction (desk scope discipline, §4 of the correction draft). | (a) audit every §26 status mark against the shipped package by execution, correcting in one pass; (b) audit the whole manual, not just Chapter 26. | **(a)** first — Chapter 26 is where the marks are densest and where one was already proven wrong. A mark that cannot be produced by executing the shipped package is stale by definition. | the OF-16 correction draft §6; [PR for OF-16/OF-17] | **OPEN** · **FORM-PRIMACY IS PART OF THE DEFECT (2026-07-26, external-AI probe):** a model reading the manuals produced a Manifold the shipped parser REJECTS on three construct types (MANIFOLD without VERSION, LEVEL without `= <column>`, MEASURE without `FROM … AS …`) — while getting HIERARCHY right, which is the §26.6 correction working in the wild. Root cause is structural, not typographical: Chapter 26 presents LONG forms as primary and the SHIPPED short forms as parentheticals, so a careful reader learns the wrong one. The audit must therefore cover FORM PRIMACY, not only status marks. |

| OF-19 | 2026-07-26 | **Stale pinned version references in the manual.** "0.7.8" appears as a hard-coded version throughout Chapter 26's annotations (and likely beyond). The OF-16 pass dropped it in the one clause it touched, in favour of "the shipped keyword set", precisely so that sentence cannot rot again. | **One clause fixed, the rest left.** Every other pinned "0.7.8" still reads as a live claim about the current release. | (a) sweep pinned versions out of prose where the claim is really "what ships now"; (b) keep the numbers and add them to a release checklist to bump. | **(a)** — a version number in prose is a claim that goes stale silently, and nothing in CI reads prose. Prefer unpinned phrasing over a bump-list nobody runs. | the OF-16 correction draft §6; [PR for OF-16/OF-17] | **OPEN** |

| OF-20 | 2026-07-26 | **Two §26.6 clauses may be spec-ahead-of-code.** The correction PRESERVED, verbatim, two claims neither the desk nor CC could ground in shipped code: that scan parameters `reset`/`within`/`step` resolve along a hierarchy (no such Frame-QL surface found in `frameql.py`), and that redundantly declared diamonds are checked for commuting (no such check found in the shipped adjudicator). | **Preserved as written.** They were carried over because the correction's scope was the inversion + the signature, and silently deleting doctrine is worse than carrying an unverified clause — but they are now flagged rather than assumed true. | (a) verify each against the shipped package; if unimplemented, they are doctrine gaps (DG rows) and the manual should mark them SCHEDULED; (b) leave as aspirational prose. | **(a)** — the manual should not assert as shipped what cannot be executed; that is the exact defect class OF-16 just closed. | surfaced in CC's OF-16 draft (open questions 4 and 5), carried into the desk draft's preserved body | **OPEN** |


| OF-21 | 2026-07-26 | **Should the map layer live in a SEPARATE FILE from the logical Manifold?** A `.cml` document today co-locates two layers: the LOGICAL declarations (what the Manifold is; what agents see over describe) and the MAP clauses (`= <column>`, `FROM <table> AS <agg>(<expr>)`, `VIA <table>(a,b)`, `REJECT …`) which are engine-visible and map-side only. | **Co-located, insulation enforced at the DESCRIBE BOUNDARY.** §2b / C-2: describe emits logical names only — no `realized_by`, no VIA bridge, no FROM table, no expression — and a standing insulation test asserts physical identifiers never cross describe or the wire. So the guarantee holds, but it is TEST-enforced at the boundary rather than STRUCTURAL at the document. | (a) split the map into its own file — insulation at the DOCUMENT boundary (Huayin's stated intent); (b) keep co-located and rely on the describe-boundary test. | **Desk will draft the fork document.** FOR (a): Manifold portability (one logical model, many maps — environments, warehouses, customers), structural rather than test-enforced insulation, and a strong design-partner story. AGAINST: locality (a level's binding no longer sits beside its declaration) and two-file sync. Rowed now so it is not lost. | external-AI probe (2026-07-26); the grammar-page proposal (workstream B); §2b / C-2 insulation + the standing insulation test | **OPEN** |

| OF-22 | 2026-07-26 | **A meta-refresh redirect stub returns HTTP 200, so crawlers can hold retired copy indefinitely.** Two of three external assistants quoted PURGED "metrics engine" positioning — traced to the retired `/launch` page. Nothing on the live site says it (verified; the byte-preserved archive sits unserved in `src/content`). | **Astro `output: 'static'` cannot emit 3xx**, so every configured redirect ships as a 200 stub with meta-refresh + canonical + noindex. Browsers and well-behaved crawlers follow it; the 200 status is what lets stale copy persist in an index or a model's training slice. | (a) real 308s via `vercel.json` edge redirects (AUTHORIZED 2026-07-26, blocked pending owner infra approval); (b) accept the stubs. | **(a).** This finding upgrades the 308 from a nice-to-have to a live misinformation vector: retired positioning is being repeated back to us by third-party assistants. | external-AI probe (2026-07-26); the blocked `vercel.json` PR | **OPEN** |
| OF-23 | 2026-07-26 | **DETERMINISTIC SERVING — two identical asks against identical data should produce identical bytes. They do not.** Found by the flap detector's first real run on `gen_transcript.py`. TWO defects, both in the SERVING path, not the recording path: **(a) ROW ORDER permutes** — the same seeded query returns its coordinates in a different order run to run (`cal.month 2024-09` first, then `2025-07`); **(b) 1-ULP FLOAT DRIFT reaches SERVED VALUES** — `130.30240066225167` vs `130.30240066225164` for the same coordinate, moving the column total. The recording pipeline merely made it VISIBLE. | Nothing. `gen_transcript.py` carries a ROWED exemption in `check_generator_determinism.py`, loud on every run, keyed to this row and failing if this row closes while the instability persists. | (a) canonical coordinate ordering at the WIRE's serialization boundary (likely cheap, likely core); (b) sort-before-aggregate, a declared output precision, or deterministic execution — a design fork needing a desk proposal. | **(a) now, (b) by proposal.** NOTE THE DOCTRINE BOUNDARY: 0.13.1's rule — *a value below the system's declared tolerance is noise, not a finding* — **explicitly does NOT cover (b)**. There is no declared tolerance on a SERVED MEASURE, so collapsing it would be laundering, not canonicalizing. Thread-pinning stays REJECTED as symptom suppression: it would make the RECORDING more deterministic than the ENGINE, which is a lie about the product. **This also touches the REPRODUCIBILITY STORY** — a reader regenerating exhibits meets 1-ULP mismatches against published full-precision floats, so the fix's design must answer for the papers' "regenerable by the reader" claim. | the flap detector, 2026-07-26 (deploys for #104/#105); **Open Planner beat 1 (F4)** — the `split` face reproduces the same class independently (1.4e-16 relative, one ULP, 2 of 12 categories, grand total stable); **the two planner doors, 2026-09-11 (Frame-QL 1.0 expression unit)** — a THIRD independent reproduction, and the first at the ask surface rather than in a generator: the builder API and the statement path serve `avg(revenue @ {day}) AT {cal.month}` with identical keys, schema and cardinality but `2810.808928571429` vs `2810.808928571428` on 1 row of 24 — the same rows reaching the same reduction in a different ORDER, and float addition is not associative. Reproduced on an idle box, and only under the combined-root `pytest packages/columna-core packages/columna-server` invocation, not the per-package one CI runs. Recorded because it moves the row's reach: (b) is not confined to the recording pipeline or to one face — two ENTRY POINTS into the planner disagree at the last ulp for the same utterance. It is still NOISE under the 0.13.1 doctrine and is not reported as a wrong number; `test_one_grammar_for_the_builder_api_and_the_statement_path` now compares values to 12 figures and keys exactly, with the reason written at the assertion, because bitwise summation order was never that test's claim and asserting it made it a flaky detector of THIS row) | **OPEN** — **and now load-bearing for the Open Planner.** Beat 1 records that BYTE-IDENTICAL CERTIFICATES and P-ECON's certified-plan cache ("which also yields deterministic serving") both INHERIT this row's requirement: a kernel that certifies over canonical bytes cannot be built on a serving path whose bytes move. F4 itself is correctly classified as NOISE per the 0.13.1 doctrine — reporting a 1-ULP difference as a wrong number would be false precision — so what this row inherits is the REPRODUCIBILITY consequence, not a correctness one. |
| OF-24 | 2026-07-27 | **The cache annotation wears a semantic name on the semantic channel.** Found by the Open Planner beat's P-BLIND probe (F5): on a FRESH store the first query returns `rollup_severity: none` with no disclosure; every identical ask after it returns `info` plus a **`freshness`** disclosure. Deterministic, reproducible across cold stores. The first asker receives LESS disclosure than the second for the same question on the same data. | **Truthful content, wrong channel and wrong name** (desk root-cause, 2026-07-27): the caveat's content is *"served from cache"* (`engine.py:131,488`), version-checked before serving — so EACH CALL'S DISCLOSURE IS TRUE and the values are identical. The defect is a **mislabeled, mischanneled annotation**: mechanical serving-provenance wearing the semantic name FRESHNESS on the semantic caveat channel. NOT launch-blocking — identical values, truthful content, immaterial grade. | (a) rename and rehome the cache annotation out of the semantic caveat channel; (b) suppress it at same-version; (c) leave it. | **(a).** And the row's DESIGN HALF FEEDS THE PROGRAM: the kernel's disclosure projection gains a **two-channel split** — *semantic* (call-invariant; **P-BLIND's true jurisdiction**) vs *mechanical* (legitimately variant, possibly not wire-worthy at same-version). P-BLIND as formalized forbids dependence on ATTEMPT COUNT; once the channels are split, the semantic channel can satisfy that literally while the mechanical channel is free to vary, because it was never a claim about meaning. | Open Planner beat 1, F5 (`specs/open_planner/BEAT_1_REPORT.md`, `fixtures/d5_p_blind.json`); `engine.py:131,488` | ~~**RULED (a) & LANDED 2026-08-31**: shipped in `columna-core` 0.18.0, wire contract `"3"` -> `"4"`. `Disclosure` now carries two channels. `caveats` is SEMANTIC — call-invariant, and the SOLE input to `outcome`, `rollup_severity` and materiality; `mechanical` is OBSERVATIONAL, carrying `"served from cache"` and nothing else today. Every severity and materiality property reads `caveats` and never `mechanical`, so P-BLIND's jurisdiction is satisfied STRUCTURALLY rather than by discipline — a standing test marks a mechanical caveat `critical` and asserts the frame severity stays `none`. The bump is deliberate: `freshness` MOVED off an existing field for an unchanged utterance, the same break-by-version case as WP-NAME-1. Two coupled repairs shipped with it (ledger P1-04, P1-05): warm TOUCH returned from its cache before coverage and the Φ_v dispositions were computed and so was quieter than cold, dropping a MATERIAL caveat; and the coverage shortfall was graded IMMATERIAL on a wire code whose MATERIAL slot was wired with no producer. Fixing the second without the first would have made the divergence outcome-visible — `disclose` cold, `serve` warm.~~ |
| OF-25 | 2026-07-29 | **Composite input anchor at the ask surface** (F1). An inline reduction's input anchor pins ONE level, not a product of levels: `avg(revenue @ {store*product*cal.month})` refuses at the parser (`planner.py:377`, *"single-level input anchors this build"*). Beat 1 attested that `ColumnEngine.reduce_series_to_anchor` executes composite input grain unmodified — F1 ran through it below the ask. So the restriction is a RECALL row at the ask surface, not a safety issue: an entire class of two-stage statistics is expressible in the engine but not askable from the grammar. | **Nothing.** The refusal ships. Beat 1 (F1) certifies the finding; the recall ledger on the homepage now carries `composite input anchor` (2026-07-29) between `face chains` and the fold, so the gap is public while WP-GRAIN-1 is in flight. | (a) lift the restriction — WP-GRAIN-1 proposal (`specs/wp_grain_1_composite_input_anchor_v0_1.md`) states the pin×output-anchor lattice laws (finer / coarser / same / orthogonal / face-crossing), their refusal codes, and the acceptance criterion (F1 transcript serves with disclosure); (b) leave as-is and carry the ledger row indefinitely. | **(a) as 0.13.4's headline** (Huayin, 2026-07-29). CLI renumbers. Wire contract unchanged (`contract_version` stays `"1"`); doctrine ships alongside the code — laws 1/2/3 in the proposal are the surface's owed refusals; law 4 generalizes the existing OF-2 immaterial provenance note. | Open Planner beat 1 (F1) — `specs/open_planner/BEAT_1_REPORT.md:73`, `attack_b.py:127`, `fixtures/attack_b_ir.json`; the proposal `specs/wp_grain_1_composite_input_anchor_v0_1.md` | ~~**RULED (a) & LANDED 2026-07-30**: WP-GRAIN-1 shipped in `columna-core` 0.13.4. The composite input anchor is a first-class pin; Law 1 mints `pin_coarser_than_output` (REFUSE), Law 2 mints `redundant_pin` (CLARIFY), Law 4 generalizes the OF-2 immaterial note (serve). The F1 transcript now SERVES and agrees with the below-surface IR to float precision (`attack_b.py`). The recall-ledger row `composite input anchor` was STRUCK the same PR, per the ledger's own rule (row down ⇔ code up). Wire contract unchanged (`contract_version` `"1"`). Rowed future finding: the composite-input × FACED-output combinatoric meets the G4 chain guard and refuses `chained_crossing` — not yet served.~~ |
| OF-26 | 2026-07-30 | **Composite pin × faced output — the chain-guard corner (WP-GRAIN-1 residue).** WP-GRAIN-1 shipped the composite input anchor (0.13.4) and struck the `composite input anchor` recall-ledger row — the headline capability serves. But one corner survives: a composite pin that includes a FACED output coordinate — `sum(revenue @ {product*category.touch}) AT {category.touch}` — resolves the inner at a base+faced composite grain, which the existing **G4 chain guard** refuses `chained_crossing`. A lawful shape we cannot yet DERIVE, given a NAMED refusal, never a silent number. Rowed out by the WP-GRAIN-1 scope table before the release; entered here (and as its own dated recall-ledger row `composite pin × faced output`) because a struck row must not silently swallow a surviving sub-gap. | **Nothing — refuses honestly.** The chain guard names it (`chained_crossing`); the plain faced output (`revenue AT {category.touch}`) serves unchanged; no silent number, no regression. | (a) license composite-input × faced-output resolution — new engine work (the crossing propagates to the pin; disclosure-stacking across a face at a base+faced grain is currently undesigned, the same G4 frontier that gates chained face crossings); (b) leave rowed — the corner stays a named refusal until a real case demands it. | **(b) for now** (Huayin, 2026-07-30, at the 0.13.4 gate): doctrine-landed / execution-deferred. Forcing new engine work into a green release to chase one acceptance line is the discipline we keep; the guard doing its job on a rowed-out corner IS the system working. Lift to (a) when a real case arrives. | WP-GRAIN-1 acceptance criterion 4 (Law 3) — `specs/wp_grain_1_composite_input_anchor_v0_1.md`; the G4 chain guard `engine.py` `_resolve_faced` (`chained_crossing`); `test_inline_reduction.py::test_law3_composite_faced_pin_refuses_at_the_chain_guard`; **beat-3 measurement** `specs/open_planner/map2/pilot_c3.py` | **OPEN** — deferred at the 0.13.4 gate; carries a recall-ledger row until derived. **MEASURED BOUNDARY (beat 3, 2026-08-01, ratifier word)**: the C3 CROSS-bearing pilot fixed the corner's edge *by execution* — **a single faced coordinate is the maximal expressible CROSS seam at v1**. Every richer shape refuses `chained_crossing`: a second anchor dimension beside a faced coord (`{category.touch, cal.month}`, `{category.touch, region}`) AND a composite input pin under a faced output (`sum(revenue @ {product*cal.month}) AT {category.touch}`) — all G4. So the vertical-seam pilot necessarily ran *at* this boundary (`revenue AT {category.touch}`: home CROSS + lowered substrate sum) and passed; a richer C3′ becomes expressible only when chained-crossing licensing ships (future WP — the beat-3 handoff wall applies, no improvised transport). The corner stays OPEN as a licensing gap, now with a measured edge. |
| OF-27 | 2026-07-30 | **Column identity elides the input anchor — the pin-eliding default (WP-NAME-1).** The §4 mechanical default names an unaliased single reducer `<reducer>_<measure>` and, in the manual's own words (`docs/frame_ql_manual_v1.md:149`), *"the input anchor does not affect the default name."* Per the Two Anchors law a pinned reduction's identity is (reducer, input anchor, output anchor); the default keeps two of the three and names half the denotation. WP-GRAIN-1 turns the elision from lossy to COLLIDING: the two F1 asks `avg(revenue @ {store*product*cal.month})` and `avg(revenue @ {customer*store*product*day})` both default to `avg_revenue` and the collision guard (correctly, never suffixing) refuses them in one frame — the guard catching the default's lie. | **Nothing yet — proposal filed** (`specs/wp_name_1_column_identity_v0_1.md`), queued behind 0.13.4. The default still ships; WP-GRAIN-1 landed without touching naming, so the collision guard is what currently stands between the default and a wrong label. | (a) retire the default: column identity = canonical expression \| AS-alias, computation provably unaffected (name is metadata), collision guard stands, `contract_version` bumps `"1"`→`"2"` (a changed default key for the same utterance); (b) keep the default and accept the elision + collisions; (c) a narrower fix that appends the pin to the default name (rejected in the filing — it re-introduces the suffix habit the guard forbids). | **(a)** — filed as WP-NAME-1 (2026-07-30). The bump is deliberate: it changes an existing wire field's value for an unchanged utterance, the canonical break-by-version case (contrast WP-GRAIN-1, which added reason codes with no bump). Acceptance = the wire-visible-blast-radius regeneration checklist green end to end, only the name changing. | the WP-GRAIN-1 collision surfaced at 0.13.4; the proposal `specs/wp_name_1_column_identity_v0_1.md`; the Two Anchors law `specs/reference/two_anchors_paper_v1_1.md`; `docs/frame_ql_manual_v1.md:149` | ~~**RULED (a) & LANDED 2026-07-30**: shipped in `columna-core` 0.14.0. The mechanical default is retired; an unaliased series is keyed by its CANONICAL EXPRESSION (`avg(revenue @ {day})`, `revenue`, `revenue.sum` — the dot-to-underscore mangle retired too). All three opens ruled: (1) leaf case uniform — no invented names anywhere, member access ships verbatim `revenue.sub`-style; (2) vehicle = its own minor 0.14.0 (a `contract_version` bump is what a pre-1.0 minor signals), server stays 0.8.2 (wire-schema text lives in core, re-exported); (3) migration note teaches the principle — key on AS aliases, author-owned, never changed by any future rule. `contract_version` `"1"`→`"2"`. Acceptance met: blast-radius regeneration green, only names changed, values byte-identical.~~ |
| OF-28 | 2026-08-23 | **Implementation vocabulary vs the ToD v6.1 terms.** ToD v6.1 §1.2 retires `member` from the core ontology — `measure family` is the family, `measure` is that family at one anchor (`F@A`) — and permits downstream implementations to retain v5 vocabulary during migration. Columna retains `measure`, `member`, `family`, `MeasureColumn`, `FamilyMember` across runtime, wire, and manual. What is unruled is whether that retention is permanent. | **Retention, unchanged.** Ratified by the v6 runtime checkpoint §11 ("No renaming. `MeasureColumn`, `FamilyMember`, `family`, `member` all stay exactly as they are"); the Frame-QL Manual states the seam in prose instead (Preface, *The Theory of Data*), which changes no token. | (a) rename the implementation vocabulary to the v6 terms; (b) an alias bridge, both vocabularies addressable; (c) deliberate permanent retention, the seam documented forever. | **Undecided — and it gates one thing: no public governed-publication authoring surface opens while the implementation vocabulary decision remains unresolved.** **UNIT D OPENED 2026-08-31** (`consolidated_ledger_v0_1.md`) — the v5→v6 crosswalk is exactly this decision, scoped as a document with no implementation authorized. This row is struck when the crosswalk is ruled, not when code moves.** An authoring surface mints governed objects under whichever vocabulary it exposes, and that choice is not reversible by documentation afterwards. Must not evaporate. | `docs/architecture/tod_v6_runtime_reconciliation.md` §§2, 10, 11 (ratified, merge `ffce9c1`, PR #175); *The Theory of Data* v6.1 §1.2 (the DOI pin lives in the Manual's Preface, not here — a fork ledger is not a publication authority); the Manual reconciliation design pass, 2026-08-23 | **OPEN** |
| OF-29 | 2026-08-26 | **The evaluation-spend guard in `run_eval.py`.** The governing rule (Huayin, 2026-08-26) is: registry/corpus/deposits/index changed -> deterministic verification only, zero model spend; agent-facing surface changed (prompt, retrieval behaviour, provider/model, review rubric, or anything else that can materially change generated or reviewed behaviour) -> evaluate once, affected cases only; nothing agent-facing changed -> no evaluation run. Today it is an instruction only, and instructions are the brittle joint — the harness itself has no idea whether the agent changed. | **Nothing shipped.** The rule is written into `services/ask/README.md` and governs by hand. | (a) `run_eval.py` refuses to run unless the agent-facing surface has changed since the last recorded run, with an explicit human override; (b) leave it as an instruction. | **(a), at the next legitimate touch of the eval harness — NOT as its own errand** (Huayin, 2026-08-26). Two riders from the ruling: the override must be EXPLICIT and human-authorised, and it must be VISIBLE IN THE EVALUATION RECORD rather than silent — a rerun that does not say it was a deliberate rerun is exactly the reflex the guard exists to stop. Design note: 'agent-facing surface' should be a recorded digest of the files that can change generated or reviewed behaviour, not a timestamp — a timestamp makes a whitespace edit look like an agent change. | Ask evaluation F (`specs/f_evaluation_report_v0_1.md`); the v1.1 reconciliation (`specs/certainty_v1_1_reconciliation_report_v0_1.md`, PR #232) | **OPEN — banked** |
| OF-30 | 2026-08-31 | **Query-level `count(*)` has no determinate analytical object.** A `UNIVERSE` declares coordinates, **not a fact table** — only a `MEASURE` carries `FROM <table>` — so a bare `count(*)` **as a series in `SELECT`** does not name what is being counted. Three readings are open and they are different numbers: (a) the **physical source-row count**; (b) the count of **existing analytical points**; (c) the count of **observations of some measure**. This is a real architectural fork, not documentation commentary: it decides whether Frame-QL has an implicit relation. | **Nothing shipped, and that is the choice.** The form does not parse into a served series; §4.2 and §6.2 are marked `[ROADMAP — the count(*) series only]` and the Manual states in prose that it does not choose among the three readings. Rowed here 2026-08-31 on Huayin's instruction, because until now the question lived **only** in the Manual with no ledger or fork entry — *"a real architectural fork, not merely documentation commentary"*. | (a) physical source rows; (b) existing analytical points (the universe's realized coordinates); (c) observations of a named measure — i.e. require `count(<measure>)` and refuse the bare form permanently; (d) admit it only where a `FROM`-bearing object is in scope. | **Do not resolve by inheritance.** SQL can answer `count(*)` because a SQL query always has a relation in hand; Frame-QL does not, and adopting the SQL reading would import an implicit analytical object the Manifold never declared — **a fact table by the back door**. Picking one silently is how a number acquires a meaning nobody declared. Note that **`AS count(*)` in a `.cml` MEASURE is a different and established case** and is unaffected: there the source table is declared on the measure. Resolving the query-level form is a language ruling, not a parser fix. | `docs/frame_ql_manual_v2.md` §4.2 / §6.2 (Second-Edition sync, 2026-08-31); *Measure Algebra Design Record v0.3* §11.1 (O6) | **OPEN** |
| OF-31 | 2026-08-31 | **The Manual still names the future multi-input surface "Column Algebra".** Design Record v0.3 **retires** that term — a column is a material carrier, not the governed analytical object — and adopts *Measure Algebra*. The Manual's §2.1 sync note calls the shape *"the leading candidate for the future **Column Algebra** surface"*, so a retired term survives in a shipped document. | **Left as-is, deliberately.** Huayin, 2026-08-31: the wording *"should eventually be reconciled, but I would not interrupt CC's current record-writing work merely to change the label."* No Manual edit was made. | (a) reconcile at the next legitimate touch of that section; (b) a dedicated wording pass; (c) leave until the surface is actually built, and rename with it. | **(a)** — it is a label, the claim under it is correct, and a standalone errand to change one noun is not worth a Manual revision. Fold it into whatever mission next touches §2.1. Note the scope is narrow: this is the *only* live use of the retired term in a shipped surface; the Mission 1 reconciliation and Design Records v0.1/v0.2 keep it as historical record by design. | *Measure Algebra Design Record v0.3* §1.2 statement 5; `docs/frame_ql_manual_v2.md` §2.1 sync note | **OPEN — banked** |
| OF-32 | 2026-09-11 | **The 1.0 grammar reads four operators the canonical authority still calls `proposed`.** `specs/frameql_capabilities.toml` gives `disjunction` (`or`), `negation` (`not`), `between` and `membership` (`in`) `standing = "proposed"`, and until this release that matched the surface: none of them parsed. The Frame-QL 1.0 precedence ladder (specification §15) places all four on its comparison and logical rungs, and `columna_core.expr` reads them — so the shipped grammar now accepts constructs the language has not ratified. This is the realization-exceeds-standing class the capability authority already names, at a seam that authority cannot see: it measures the OPERATOR REGISTRY, and these four are GRAMMAR. | **Standing left at `proposed`, deliberately.** A parser reading a construct does not ratify it, exactly as a build running one does not — the same rule the authority already enforces against the registry. Appendix B of the Manual now reserves all five expression keywords (`AND` was already ratified), which is a statement about the grammar and not about standing, and the four capability notes say so at the authority itself. | (a) ratify all four on the strength of §15 listing them in the closure; (b) ratify selectively; (c) leave `proposed` and treat §15's ladder as grammar closure that does not by itself confer canonical standing on each operator it mentions. | **(a) or (c), and the distinction matters more than the answer.** §15 is titled *Grammar and precedence closure*; it closes where these operators BIND, which is not obviously the same act as ratifying each as a canonical capability. If the ladder is read as ratification, four rows move together and the capability tables move with them. If it is not, §15 should say so, because a reader of Appendix A currently sees `proposed` beside an operator the shipped parser accepts. Not decidable in a documentation pass. | Frame-QL 1.0 §15; `specs/frameql_capabilities.toml` (the four `note` fields, corrected 2026-09-11); Manual Appendix B; `columna_core/expr.py` `KEYWORDS` | **OPEN — and ruled (c) on the principle, 2026-09-11 (CG2)**: *"Grammar recognition of `or` / `not` / `in` / `between` does not itself ratify their portable semantic capability. Do not promote capability standing merely because the new parser reads the syntax."* So the four rows STAY `proposed`, and the reading that §15 listing an operator on a precedence rung is not by itself an act of ratification is the ruled one. What remains open is the substantive question the row was opened for — whether each operator SHOULD be ratified, on its own portable-semantics evidence rather than on the parser's reach. The seam the capability authority cannot see is unchanged and is the lasting finding: it measures the operator REGISTRY, and these four are GRAMMAR. |

| OF-33 | 2026-09-13 | **What population does `manifold_status.evidence.licenses` count?** The tool's published description says *"the evidence standing (how many adjudicated Licenses are verified / corroborated / untestable)"*. The implementation counts entries in `PublishedScope.licenses`, whose own field comment scopes it to `"derived.member" -> verdict` — a DERIVED-FAMILY snapshot — and which includes members whose license is `None` (unadjudicated). Three populations are therefore in play and the shipped code names none of them: (a) derived-family member ENTRIES including unadjudicated ones [shipped]; (b) derived-family members that actually HAVE a license; (c) every adjudicated `License` across all license-bearing categories — derived members, hierarchies, universe bases, faces — which is what "adjudicated Licenses" most naturally reads as, and which `get_evidence` already exposes separately. | **Left exactly as it was (a).** The verdicts loop beside it was repaired in the same unit (it was structurally incapable of counting anything), and the repair makes the disagreement VISIBLE for the first time: a snapshot of four entries, one unadjudicated, now reports `licenses: 4` beside `sum(verdicts.values()) == 3`. Redefining the field in the same breath would have been a silent semantic change riding a bug fix. | (a) keep entry-count, and correct the published description to say so; (b) count only entries carrying a license — the smallest change that makes the two numbers agree; (c) widen to all license-bearing categories, which changes the number for any manifold with adjudicated hierarchies or universe bases. | **(b) or (c), and the choice is a product question rather than a code one.** (b) makes the block internally coherent — every counted license has a counted verdict — at the cost of leaving hierarchy and basis licenses invisible in a field called "evidence standing". (c) matches the published wording and what a reader of "how many adjudicated Licenses" would expect, but is a behaviour change for existing manifolds and would break `gen_firstlight.py`'s build-time assertion that `evidence["licenses"] == 0`. Not decidable from the existing docs, tests or rulings — which is why this row exists rather than a commit. | [PR #286](https://github.com/datumwise/columna/pull/286); `columna_server/tools.py::manifold_status`; `columna_core/adjudication.py::_snapshot_licenses` + `PublishedScope.licenses`; `packages/columna-server/tests/test_manifold_status_evidence.py` (the current behaviour is pinned) | **OPEN** — candidate (a) RULED OUT 2026-09-14, on live-consumer semantics rather than on argument: `apps/website/scripts/gen_firstlight.py:144` reads this number as *"how many adjudicated licenses"* and dies with *"firstlight now carries adjudicated licenses"* on any non-zero. Under (a) a manifold with derived members and no adjudication would report a positive count while carrying ZERO adjudicated licenses, and that build-time guard would fire with a message that is factually wrong — so the shipped behaviour contradicts a documented reading of the public contract. The row stays open BETWEEN (b) and (c): the same consumer cannot discriminate them, because firstlight has neither derived families nor hierarchies and both candidates give 0 for it. Not to be resolved incidentally. |
| OF-34 | 2026-09-13 | **`describe_manifold.published_scope.blocked_by` collapses two lineages over one level pair.** Its key is `f"{k.frm}->{k.to}"` built from an `EdgeKey(lineage, frm, to)`, so two refuted lineages over the same level pair overwrite each other and the last one wins — silently. Its sibling `blocked_edges` collapses the same collision DELIBERATELY and says so in a comment (a dedupe: two refuted lineages over one pair are one blocked pair). `blocked_by` is the field that exists to explain WHY, so losing a lineage there loses the explanation the caller came for. | **Unchanged.** Found while mapping the read-only surface for the successor work and deliberately kept out of the `evidence.verdicts` correction: it is a different defect in a different field, and the fix changes a public key format. | (a) key by `f"{k.lineage}:{k.frm}->{k.to}"` — complete, and a wire-shape change for any consumer parsing the key; (b) keep the pair key and make the value the accumulated list across lineages (each record already carries its own `lineage`), which is additive and preserves the key format; (c) leave it, and document that `blocked_by` reports one lineage per pair. | **(b).** The records already carry `lineage`, so appending rather than overwriting loses nothing and changes no key. It is the only option that neither breaks a key parser nor keeps discarding evidence. Worth checking against the Explorer generator, which reads `published_scope`. | Read-only surface reconnaissance, 2026-09-13; `columna_server/tools.py::describe_manifold`; `columna_core/adjudication.py::PublishedScope.blocked_by` | **OPEN** |
| OF-35 | 2026-09-14 | **`KNOWN_KINDS` advertises four governed declaration kinds that have no contract, and nothing consumes the constant.** `governed/publication.py:55` lists `attribute`, `relationship`, `hierarchy` and `crosswalk` beside the three live kinds (`anchor`, `universe`, `family`). It is **never referenced anywhere in the tree** — the reader validates `kind` only against `RETIRED_KINDS`, so an unknown kind is carried as an untyped `Declaration(kind, name, body)` with no body schema, no reader and no validation. An artifact can therefore appear to use an ALLOWED governed kind while carrying no checkable governed meaning: a `hierarchy` declaration would parse, survive ingest, and mean nothing any consumer could act on. | **Unchanged**, and found the honest way — while mapping what `describe_manifold` could serve for a governed-only unit, the question "does v2 have hierarchies?" got two different answers depending on whether you read the constant or the parser. | (a) close the live kind set to the three currently governed kinds and delete the four that have no contract, so the format's vocabulary and its enforcement agree; (b) keep the four as genuinely intended future vocabulary and make them REJECTED/UNUSABLE until their contracts exist, so nothing can carry one meanwhile; (c) give the four real body schemas and readers — a format change, far beyond a constant. | **Not inferable from the constant's name, and deliberately not guessed here.** The two live readings are opposite: (a) says the list is stale and over-advertises; (b) says the list is a roadmap and the gap is that nothing enforces it yet. Both are consistent with the evidence — an unused constant cannot say which it meant. What is not in doubt is the present state: the format advertises more vocabulary than it defines, and the difference is invisible to every consumer. | Read-only surface reconnaissance for `describe_manifold`, 2026-09-14; `columna_core/governed/publication.py:55` (`KNOWN_KINDS`), `:59` (`RETIRED_KINDS`, the only kind check the reader performs) | **OPEN** |
| OF-36 | 2026-09-14 | **A governed-v2 unit has no descriptive surface of its own.** Three read-only tools have now been ruled on: `discovery` is governed-native, while `describe_measure` is governed-native only by OMITTING representation/missingness/evidence, and `manifold_status` and `describe_manifold` remain legacy-model surfaces that return `no_legacy_model`. So the publication's richest facts — C1 target, participation, continuation law, sufficient-state basis, per-responsibility STANDINGS, constitution fingerprints, ratification authority — have **nowhere to go on any public surface**, not because they are unfit to publish but because every existing tool is shaped around the legacy model. | **`no_legacy_model` for the two legacy-shaped tools, and silence for the governed facts.** The correct provisional answer at every step — nothing was fabricated to fill a legacy shape — but it leaves governed constitution unpublishable rather than merely unpublished. | (a) a new governed descriptive surface shaped around the governed object as it actually exists — family/universe/anchor constitution, the nine responsibilities and their standings, sufficient-state basis, participation, continuation; (b) extend the existing describe contracts with governed blocks, which would make those tools two shapes wearing one name; (c) leave governed units describable only through `discovery`. | **(a), when it is ruled — and it must be designed as its own shape rather than derived from the legacy one.** The lesson of this line of work is that the legacy geometry shape cannot carry governed facts without either fabrication or near-total omission; a surface built around responsibilities and standings would carry them natively. RECORDED, NOT DESIGNED (Huayin, 2026-09-14): this row exists so the absence is a known state rather than an oversight, and explicitly does not authorize designing it. | `describe_manifold` decision reconnaissance, 2026-09-14; [#287](https://github.com/datumwise/columna/pull/287) (`discovery`), [#288](https://github.com/datumwise/columna/pull/288) (`describe_measure`, the omission rule) | **OPEN** |
| OF-37 | 2026-09-14 | **The in-tree describe-consumer inventory has drifted — and it is the mechanism that exists to prevent exactly this.** `disclosure_wire.py:138-154` maintains a *"KNOWN IN-TREE DESCRIBE CONSUMERS (keep current)"* checklist whose stated purpose is load-bearing: the removal policy for the describe contract *"rests on an in-tree-consumers premise"* — a field may be removed without a version bump only while every consumer is inside the repo and **ENUMERABLE**, so *"the next wire change gets a CHECKLIST instead of a grep"*. The list was written in response to the 0.13.0 ASSERT retirement, where a Python-shaped sweep missed the Explorer's TypeScript declaration and **the site build caught what the inventory did not**. It has since drifted three ways: (i) it lists `apps/website/scripts/gen_case.py`, which contains **zero** occurrences of `describe`; (ii) it **omits** `apps/website/scripts/gen_transcript.py`, which passes the ENTIRE raw wire through to the Explorer as `explorer_describe` (`:189-192`) and itself hard-subscripts `d["dtype"]` (`:84`) and `d["provenance"]["measure"]` (`:88`); (iii) its one agent entry names a DIRECTORY (*"the demo agents"*) rather than the path that carries the risk — `agent/loop.py:51-66`, where `render_manifold_context` builds the agent's system prompt by **hard subscript on six keys**, so a missing key aborts agent CONSTRUCTION rather than degrading a rendering. | **Unchanged, and nothing swept.** Found while sweeping describe consumers for the `describe_manifold` governed-surface decision — the sweep the checklist exists to make unnecessary, and which found both more consumers and fewer than the checklist lists. | (a) correct the list in place — strike `gen_case.py`, add `gen_transcript.py`, and name `agent/loop.py:51-66` in place of the directory; (b) replace the manual checklist with an **executable/checked** mechanism — a test or CI guard that enumerates describe consumers and fails when one drifts, so the in-tree-consumers premise is CHECKED rather than asserted; (c) keep the manual list, accept drift as its running cost, and rely on the site build to catch TS-side omissions as it did in 0.13.0. | **The ruling question is whether a manually maintained checklist is sufficient at all, or whether the consumer set needs an executable mechanism** — not which three entries to fix. (a) restores the list to true in one pass and leaves the drift mechanism untouched, so it will drift again; the present drift is itself the evidence for that. (b) is the only option that makes the premise checkable, but it is real design work: the consumer set spans Python and TypeScript, build scripts and runtime, and *"consumes describe"* is not a mechanically obvious predicate — `gen_transcript.py` qualifies by passing the wire through untouched. Not decidable in a ledger pass. What is not in doubt is the present state, and it is the defect: **the mechanism intended to enumerate and protect consumers of the describe wire has drifted, so wire-change review can no longer rely on that inventory.** | describe-consumer sweep, 2026-09-14; `columna_core/disclosure_wire.py:138-154`; `apps/website/scripts/gen_case.py` (listed, zero `describe`); `apps/website/scripts/gen_transcript.py:84,88,189-192` (unlisted); `packages/columna-server/src/columna_server/agent/loop.py:51-66` (`render_manifold_context`) | **OPEN** |
| OF-38 | 2026-09-14 | **The Explorer consumes a retired scope field, so the public site asserts "full scope published" unconditionally, forever.** `cut` / `cut_by` were **removed from the producer in 0.13.0** with the ASSERT retirement — `tools.py:322-325` records the ruling (*"the cut region's sole producer was a violated assert, so the two fields could only ever be empty"*) and the wire now carries only `blocked_edges` and `blocked_by` (`:331-332`). `apps/website/src/explorer/manifold-explorer.ts` still **declares** `published_scope: { cut: string[]; blocked_edges: string[][]; cut_by?: any; blocked_by?: any }` (`:19`) and still **reads** `D.published_scope?.cut \|\| []` (`:104`). The optional chain and the `\|\| []` fallback mean the key is now permanently absent, the array permanently empty, and the ternary permanently takes its else branch: every render, of every manifold, prints *"full scope published"*. `blocked_edges` and `blocked_by` are on the wire AND on the interface, and **neither is consumed for that display** — so a manifold with a refuted hierarchy and genuinely blocked transport is published to the public site as having its full scope published. The Explorer is server-side rendered at build (`explorer.astro:10`), so this is shipped HTML, not a browser-time detail. | **Unchanged.** Found in the same describe-consumer sweep; consumer-side, and this batch is ledger-only by instruction (Huayin, 2026-09-14). | (a) compute the scope note from the fields that SURVIVE — report blocked edges, and their `blocked_by` reasons once OF-34 is settled — and reserve *"full scope published"* for a genuinely empty `blocked_edges`; (b) drop the retired `cut`/`cut_by` from the interface and render NOTHING when there is no scope information, removing the false claim without adding a true one; (c) leave it, on the grounds that no shipped manifold currently carries a blocked edge. | **(a) — and the framing is the point: this is a PUBLIC-DISPLAY CORRECTNESS defect, not stale typing.** Deleting three dead identifiers from a TypeScript interface is housekeeping; what is actually wrong is that the site makes a **positive assertion about published scope that its own data cannot support**, on the only surface a reader outside this repo can see. (c) is the tempting reading — the claim is accidentally true for today's demo data — but it is true by coincidence, and the first blocked edge makes the site wrong with nothing in the Explorer having changed; that is the signature of a latent correctness defect rather than a cosmetic one. Note the dependency and the distinction: (a)'s richer form reads `blocked_by`, whose two-lineage collision is **OF-34** — the rows should be read together, but they are different defects in different layers (OF-34 is the producer collapsing evidence; this is the consumer never reading it). | describe-consumer sweep, 2026-09-14; `apps/website/src/explorer/manifold-explorer.ts:19,104-107`; `columna_server/tools.py:322-332` (the 0.13.0 retirement comment + the surviving wire); `apps/website/src/pages/explorer.astro:10` (build-time render); distinct from OF-34 | **OPEN** |
| OF-39 | 2026-09-14 | **The successor proofs stamp retained state with the WRONG FAMILY's constitution fingerprint, and the standing object's own comparability rule is why nobody noticed.** `packages/columna-platform/exhibit.py:15` and `tests/conftest.py:89` pin `CONSTITUTION = "fcf-1:c176d2a4…"` as a literal and carry it into every `Standing` built for **revenue** (`fam_qv8Ky3mR7bTpZa1LwXcNdg`). In the lighthouse publication's `authority.family_constitution`, that fingerprint belongs to `fam_Hs2VnE9QjLkYbW0uPcAtRf` — **`count(revenue@sale_at)`**. Revenue's own constitution fingerprint is `fcf-1:3569904f…`, present in the same artifact and read by nothing. So Proof A, B and C each retain revenue's material state under a neighbouring family's constitution. TWO DEFECTS, AND THE SECOND IS THE DURABLE ONE: (i) the fingerprint is hand-pasted rather than read from the governed artifact, where `ConstitutionAuthority` (`columna_core/governed/publication.py:298-301`) already carries it per `family_id`; (ii) **it has never failed a test because `Standing.comparable_to` (`columna_platform/state.py:63-68`) returns `(constitution_scheme, participation, basis, realization)` — the SCHEME, and not the FINGERPRINT.** `RetainedStateStore.combine` (`state.py:162`) is the tuple's only consumer, so two states carrying DIFFERENT governed constitutions under one scheme compare as compatible and may be folded together. The wrong value is inert, which is exactly why it survived. | **Unchanged, and rowed before any repair (Huayin, 2026-09-14).** Found while mapping the execution boundary for the first material-execution slice, not by a failing test — no test could have found it, for the reason in (ii). | (a) read `authority.family_constitution[family_id]` from the publication and delete the literal — corrects the value, leaves the comparability rule as it is; (b) (a), and additionally move `constitution` INTO `comparable_to`, so a fingerprint mismatch blocks combination as a stale state should; (c) leave `constitution` out of comparability deliberately and DOCUMENT that the field is disclosure-only, so a reader does not assume it gates reuse. | **(a) is a correction and is not in doubt; the choice between (b) and (c) is the real fork and is a governed question, not a code one.** The SSE contract's invariant is *comparability plus conservative invalidation*, and a fingerprint is called *"a permitted representation, not the invariant"* (`state.py:44-46`) — which argues for (b), because a state constituted under a superseded constitution is precisely what conservative invalidation exists to catch. Against (b): `comparable_to`'s docstring says two standings must be able to be INCOMPARABLE rather than merely unequal, and a fingerprint whose scheme matches but whose value differs is unequal-and-comparable, which may be the wrong reading of a constitution change. Not decidable here. Note the scope: today nothing in the shipped server path calls `combine`, so (b) would change no current behaviour — which makes it cheap to do and easy to keep deferring. | execution-boundary reconnaissance for the first material slice, 2026-09-14; `columna-platform/exhibit.py:15`, `tests/conftest.py:89`; `columna-platform/src/columna_platform/state.py:63-68` (`comparable_to`), `:157-176` (`combine`, its only consumer); `columna-core/src/columna_core/governed/publication.py:298-301` (`ConstitutionAuthority`); `columna-core/tests/fixtures_v2/lighthouse-v2-publication.json` (`authority.family_constitution`) | **OPEN** |
| OF-40 | 2026-09-14 | **The realization reader states a guarantee — that an unknown `family_id` is refused — which no code in the tree enforces.** `columna_core/compiler/realization.py:22-24` says the reader *"never computes one, never falls back to a name, and refuses a realization whose `family_id` the publication does not declare"*, and the ratified realization-v2 freeze §4 states the rule as law: *"Missing, duplicate and **unknown** all refuse"* (`docs/architecture/core_realization_profile_v2_freeze.md:154`). **Missing and duplicate do refuse; unknown does not.** `compile_v2.py` iterates `mapping.families` exactly once (`:179`, inside `_realization_endpoints`, which checks only R1/R2 endpoint facts), and its totality check (`:465-466`) is computed over `publication.families` — so it detects a family with no claim and never a claim with no family. There is no reverse pass. The ANCHOR side has precisely the check the family side lacks (`compile_v2.py:291-294`, *"mapping realizes a component of anchor {…}, which the publication does not declare"*), which is what makes the omission legible as an oversight rather than a policy. A `family` realization naming a `family_id` the publication never declares is therefore accepted, its endpoint still subjected to R1/R2, and its identity claim never answered. The reader ALONE cannot do this — it never sees a publication — so the guarantee as worded can only ever be discharged by the compiler, and the sentence attributes it to the wrong component. | **Unchanged, and rowed before repair (Huayin, 2026-09-14).** Found by reading the freeze against the implementation while scoping the first material slice. No test covers the case in either direction. | (a) add the reverse pass in the compiler and leave the reader's wording alone, treating `realization.py:22-24` as describing the FORMAT's guarantee rather than the module's; (b) (a), plus correct the docstring to name the compiler as the enforcer, so the sentence stops attributing a check to a module structurally incapable of performing it; (c) treat the sentence as aspirational and row the gap without repairing it. | **(b).** (a) closes the behavioural hole and leaves a module asserting something about itself that remains untrue; the docstring is load-bearing here because it is the stated reason a mapping edit cannot be a family succession — *"it cannot change which family is being realized — it can only fail to match one"* — and a reader auditing that claim will look in the reader, find no such check, and have to reconstruct where it actually lives. (c) is not available: the freeze is RATIFIED and states the rule, so the gap is a conformance defect against a ruling rather than an open design question. The narrow judgement worth recording is that **a prose guarantee in the wrong module is a distinct defect from the missing check**, and closing only the second leaves the first. | execution-boundary reconnaissance for the first material slice, 2026-09-14; `columna-core/src/columna_core/compiler/realization.py:22-24`; `columna-core/src/columna_core/compiler/compile_v2.py:179`, `:291-294` (the anchor-side check that exists), `:465-466` (totality over `publication.families`); `docs/architecture/core_realization_profile_v2_freeze.md:154` (§4, RATIFIED) | **OPEN** |
| OF-41 | 2026-09-14 | **A mis-provisioned test environment degrades into 34 ordinary test failures instead of one setup error.** `packages/columna-server/pyproject.toml:72` sets `asyncio_mode = "auto"`, and the async tests depend on `pytest-asyncio` to honour it. When the plugin is ABSENT, pytest does not stop: `asyncio_mode` becomes a `PytestConfigWarning: Unknown config option`, and each of the 34 `async def` tests then fails individually with *"async def functions are not natively supported"*. The suite reports `34 failed, 350 passed` — the shape of a real regression — for what is actually an un-provisioned interpreter. **THE DECLARATION IS NOT THE DEFECT, AND THE FIRST FRAMING OF THIS ROW WAS WRONG.** `pyproject.toml:55` DOES require the plugin (`test = ["pytest>=8.0,<10.0", "pytest-asyncio>=0.23,<2.0"]`) and CI DOES install it (`.github/workflows/ci.yml:58`, `pip install -e "packages/columna-server[test]"`), so neither the extra nor the pipeline is at fault and no dependency needs adding. The defect is narrower and is about DIAGNOSIS: nothing makes the mis-provisioned case announce itself, because no package sets pytest's `--strict-config` (which promotes an unknown config option to an error) and none sets `filterwarnings = ["error"]` — verified absent from every `pyproject.toml` in `packages/`. Found by hitting it: a container that installed the packages with `--no-deps` and never installed the `[test]` extra produced exactly those 34 failures, and they were attributable to an in-flight change only because the same 34 reproduced on an unmodified `main`. | **Unchanged, and deliberately not fixed** (Huayin, 2026-09-14): rowed in the workstream that hit it, repaired in none. | (a) add `--strict-config` (or `filterwarnings = ["error"]`) to the server's `pytest.ini_options`, so an unknown config option halts with one message naming the missing provision; (b) leave it, on the grounds that every supported path — CI and a correct local `[test]` install — already provides the plugin, and the failure mode is only reachable by installing the package wrongly; (c) make the config strict across ALL packages rather than the one where the symptom happened to appear, since the property is a property of pytest and not of this suite. | **(a) or (c), and the distinction is whether this is a server fact or a repository fact.** The argument for acting at all is not that the failure is likely — it is that this failure mode is INDISTINGUISHABLE FROM A REGRESSION at a glance, and the repository's whole disposition is that a defect must fail in its own jurisdiction rather than borrow another's. Thirty-four failures in the async tests is a setup fault wearing a test fault's clothes, and the cost of telling them apart was a full second run against an unmodified tree. (b) is honest about likelihood and wrong about consequence. (c) is the wider reading and is probably right — nothing about the property is server-specific — but it changes three files to fix a symptom observed in one, which is a judgement about repository-wide policy rather than a repair. | observed while verifying the realization-v2 conformance predecessor, 2026-09-14 ([#293](https://github.com/datumwise/columna/pull/293)); `packages/columna-server/pyproject.toml:72` (`asyncio_mode`), `:55` (the extra, which is CORRECT); `.github/workflows/ci.yml:58` (CI installs it); no `--strict-config` or `filterwarnings` in any `packages/*/pyproject.toml` | **OPEN** |
| OF-42 | 2026-09-14 | **A realization whose material is split across source objects cannot be executed, and there is no governed account of when it should be.** The first material slice assembles one analytical point from ONE material object: `source.read_anchored` requires the family's value endpoint and every anchor-component endpoint to resolve to the same `(connection, schema, table)`, and raises `UnsupportedByThisProfile` otherwise. That is the honest present answer — assembling a point from two objects is a JOIN, and this profile implements none — but the format permits the arrangement: each `anchor_component` realization carries its OWN endpoint, so a mapping may lawfully put `store` in a dimension table and `amount` in a fact table, and K0v2 accepts exactly that (it emits a Core image whose backend does the joining). **So the split is expressible in the ratified format, accepted by one profile, and unexecutable by the other, with nothing anywhere saying under what conditions composing across objects preserves the governed meaning.** The refusal's CLASSIFICATION is the load-bearing part and is deliberate: `unsupported` (ERROR, realization jurisdiction), **not** `want_of_state` — re-realizing the same claim cannot conjure a capability, and `want_of_state`'s remedy would send an operator to re-materialize against a path that does not exist. | **Refuses as a capability limit, and rowed rather than implemented** (Huayin, 2026-09-14). Surfaced by building the first material path, not predicted by the reconnaissance that preceded it — the reconnaissance read the fixture, where every endpoint happens to name one table. | (a) implement composition in the Platform profile, with a governed account of what it preserves — at minimum, that the composition does not change which contributions participate; (b) declare the single-object constraint a PROFILE FACT and state it in the platform profile, so a mapping author learns it from the contract rather than from a refusal; (c) require the realization to say so — a composition claim the profile checks, in the same claim/check shape every other realization fact already has. | **The ruling question is not "should Platform join" — it is what governed or realization conditions permit composing material across objects, and which layer owns the answer.** A join is not neutral plumbing at this boundary: it can change the support set (an inner join drops points, an outer join manufactures absence at points that had none), and manufacturing absence from present evidence is the defect P1-31 already names in a neighbouring layer. So (a) cannot be taken as an implementation task — the capability needs the account first, or it will silently decide a participation question. (b) is honest and cheap and settles nothing. (c) is the shape that would fit this format, where every fact is a claim the compiler checks, but there is no vocabulary for it today and inventing one is a format change. Not decidable here. | first material execution, 2026-09-14 ([#295](https://github.com/datumwise/columna/pull/295)); `columna-platform/src/columna_platform/source.py::read_anchored` (the single-object requirement and its refusal); `columna-platform/tests/test_material_execution.py::test_material_in_two_objects_is_a_capability_limit_and_not_a_governed_refusal`; `columna-core/src/columna_core/compiler/realization.py` (`AnchorComponentRealization.endpoint`, per-component); ledger P1-31 (absence manufactured from present evidence) | **OPEN** |
| OF-43 | 2026-09-14 | **`columna-platform` is outside the enforced Ruff gate, and it is now on the material serving path.** CI runs `python scripts/gates.py --gate ruff-core ruff-server` (`.github/workflows/ci.yml:71`), and `scripts/gates.toml` defines exactly those two lint gates (`:45-56`). There is no `ruff-platform`. The omission is an ASYMMETRY rather than a policy: `pytest-platform` WAS enrolled when Proof A landed (`gates.toml:81-82`, `ci.yml:96-97`, with a comment explaining the step's naming), so the package is already a first-class CI citizen for tests and simply was not given the matching lint gate. It mattered less when the package was a proof that touched no data; it matters now, because the same package resolves public Frame-QL to a governed identity, reads material, and serves through the public wire. The live risk is not style — it is that `ruff check` with this repo's `F,E9` selection catches undefined names and unused imports, and the material slice's own lint error (an unused import in a new test file) was caught **only because it was run by hand**. | **Rowed, not repaired** (Huayin, 2026-09-14): rows first, and the repair returned as a recommendation rather than taken while the finding was being written. | (a) enrol Platform in the existing gate — one `[[gate]]` entry in `gates.toml` and one name added to the `--gate` list in `ci.yml`, exactly mirroring `pytest-platform`; (b) restructure the lint gates to be package-derived rather than enumerated, so the next workspace member is covered on the day it is created; (c) leave it, on the grounds that the package is unpublished. | **(a), and the reason to say so plainly is that (b) is the tempting answer and is the wrong size.** The gate structure is not the defect: it already supports per-package gates and already carries one for this package. A derived list would remove one line of future maintenance and would replace an explicit, greppable inventory with an inference — in a repository whose standing lesson (OF-37) is that a checklist which drifts is worse than one that is read. (c) is refused by the change in what the package does: unpublished is a distribution fact, and this package is now in the serving path of a public tool. Worth recording alongside: the two lint gates name DIRECTORIES explicitly (`src`, `demos`, `tests`), so a Platform entry must name `packages/columna-platform` in a way that covers `fixtures/` too — the material fixture is imported by both test suites and is not under any of the three names the existing gates use. | first material execution, 2026-09-14 ([#295](https://github.com/datumwise/columna/pull/295)); `.github/workflows/ci.yml:71` (the two lint gates) and `:96-97` (`pytest-platform`, enrolled); `scripts/gates.toml:45-56` (`ruff-core`, `ruff-server`) and `:75-82` (`pytest-platform`, with its enrolment note) | **OPEN** |
| OF-44 | 2026-09-14 | **COUNT's governed target is not representable as a machine-readable fact, so no execution profile can establish which count a family asserts without interpreting prose.** The foundation vocabulary itself names the problem: `COUNT`'s `target_form` states that §11.5.1 distinguishes **two** targets — *"`count(I)` counts participating analytical points of the anchor, `count(x@I)` counts participation under the operand construction's governed rule"* — and that *"which one a family asserts is part of its target specification and is NOT supplied by citing this law"*. `resolve.py:169-172` restates it as the standing proof that a cited law does not establish a target. **And C1 resolves to free prose:** `Standing(C1_TARGET, ESTABLISHED, DECLARED, value=fam.target)`, where `fam.target` is a `str`. The lighthouse family selects its reading in English — *"count(x@I) — the number of SUPPORTED revenue observations, not the number of participating sale points. §11.5.1 calls these distinct targets; this family asserts the former"* — which is a correct and careful constitution and is unreadable by a runtime. So the governed model carries the distinction, declares that citing the law does not settle it, and provides no fact by which an execution profile can settle it either. **WHAT MAKES THIS DANGEROUS RATHER THAN MERELY OPEN:** at coincident supported grain the two readings AGREE NUMERICALLY — one contribution per analytical point, and absent observations cannot reach the carrier because admission refuses them — so an implementation that picked the wrong reading would be indistinguishable from a correct one on every fixture this repository can currently build. That is the same concealment the admission boundary already documents for C9 (*"for SUM they are numerically coincident, which is exactly what makes the substitution invisible"*), one responsibility over. | **COUNT NOT IMPLEMENTED, and deliberately not inferred** (Huayin, 2026-09-14): *"Do not implement COUNT by inferring its §11.5.1 target from prose or from numerical coincidence in the current fixture."* The second material slice was redirected to MIN, whose target carries no such ambiguity. No integer admission was added, since adding it only to make COUNT executable would be building the machinery for a semantics nobody has selected. | (a) a machine-readable slot in the family constitution — a target discriminator beside `target`, carrying the §11.5.1 choice as a governed fact rather than as prose; (b) two distinct foundation laws, so the citation itself selects and the ambiguity cannot be expressed; (c) leave the prose and have each profile refuse COUNT as unestablished-for-execution, which is honest and makes COUNT permanently unservable; (d) treat the choice as entailed by something already present — the operand's participation law, or the presence of an operand at all — if that entailment can be shown to hold. | **The question this row exists to ask is WHERE the choice lives, not which choice is right.** (b) is the cleanest and is a vocabulary change with a version consequence: it would make the distinction unforgettable, at the cost of two law names where the theory has one law with two targets — which may be the wrong shape if §11.5.1 means one law genuinely admits two targets. (a) keeps the theory's shape and adds a slot, and the hard part is not the slot but deciding whether the discriminator is a closed enumeration (and therefore a format change every future two-target law must join) or free-form (and therefore the same problem wearing a schema). (d) is the one worth TESTING before the others are debated: if the choice is already entailed, no new carrier is needed — but it must be shown, not assumed, and the numerical coincidence above means the usual evidence will not distinguish it. (c) is the honest fallback and the current de-facto state. Not decidable here, and deliberately not decided in the workstream that found it. | second-material-slice scoping, 2026-09-14; `columna-core/src/columna_core/governed/foundation.py` (`LAWS["COUNT"].target_form`, and `result_domain="integer"`); `columna-core/src/columna_core/governed/resolve.py:169-172` (C1 is `fam.target`, a string; the comment naming COUNT as the standing proof); `columna-core/src/columna_core/governed/migrate.py:223-225` (the migration asks a human the same question); `columna-core/tests/fixtures_v2/lighthouse-v2-publication.json` (`count(revenue@sale_at)`'s prose selection); `columna-platform/src/columna_platform/admission.py` (the C9 concealment this mirrors) | **OPEN** |
| OF-45 | 2026-09-14 | **The material path verified COINCIDENT grain between two metadata layers and never against the material, so duplicate contributions at one governed point were servable as several answers to one question.** The first material slice ([#295](https://github.com/datumwise/columna/pull/295)) checked the realization's `grain` claim against the governed `contribution_structure` — a claim against a claim — and stopped there. Neither of those is the delivered rows. `COINCIDENT` means *"one contribution per analytical point"* (C4's own resolution note), which is a statement ABOUT ROWS and is checkable the moment rows exist; nothing checked it. **The consequence differed by path and both were wrong.** On the PRIMITIVE path the values were served without any fold, so two material rows at one `sale_at` point reached the public wire as TWO ROWS AT THAT POINT — two answers to one governed question, under an anchor the publication declares once, with `outcome: serve` and no disclosure. On the CONSTRUCTED path (added in [#299](https://github.com/datumwise/columna/pull/299)) the same material would have been FOLDED — a resolution performed by a profile whose governed contribution structure says there is no resolution to perform, which is the `root_evaluator` defect of v1 re-made at the admission boundary. Neither could fire on the committed fixture, whose four rows sit at four distinct points; the defect was invisible for the ordinary reason that the fixture is well-behaved. | **A repair landed in #299 as admission CHECK 5** — the `COINCIDENT` claim tested against the coordinate multiplicity of the carrier, refusing `want_of_state` when any analytical point carries more than one contribution. It sits in `admit_anchored`, so it covers the primitive and constructed paths from one place, and a control asserts the refusal on the SUM path as well as the MIN path. **The row is NOT claimed closed by that repair** (Huayin, 2026-09-14): closure follows the ledger's independent discipline, and the repair was written by the same unit that found the defect. | (a) accept CHECK 5 as the discharge and close on independent re-adjudication; (b) additionally require the ENTIRE chain to be stated as one named invariant — governed contribution structure ↔ realization grain claim ↔ delivered contribution multiplicity — so a future path cannot satisfy two links and be thought conformant; (c) widen the finding: ask which OTHER realization facts are checked only between metadata layers and never against material, and treat this row as the first instance of a class. | **(c) is the reading worth banking, and (b) is how it would be made durable.** The defect was not that a check was missing — it was that two checks LOOKED like coverage of the same fact, and the shape of the gap was invisible precisely because both metadata layers agreed. That is a class, not an instance: `exactness` is today checked as a claim against the governed domain and never against the carrier's realized precision beyond the measured envelope, and `connection`/`schema` are consumed into selection without anything confirming the object selected is the one the claim described. Whether those are gaps or correct divisions of labour is exactly the question this row's class asks, and it is not answerable by inspecting any one of them. Note the ordering constraint any repair inherits: the material-vs-claim check can only live where material exists, which is admission — the same reason the envelope refusal could not live in the compiler. | second material slice, 2026-09-14; the defect: `columna-platform` at [#295](https://github.com/datumwise/columna/pull/295) (`admission.admit_anchored`, CHECKS 3 and 4 only); the repair: [#299](https://github.com/datumwise/columna/pull/299) (`admission.py` CHECK 5) and `tests/test_material_formation.py::test_material_with_two_contributions_at_one_point_refuses_a_coincident_claim`; `columna_core/governed/resolve.py` (C4's *"one contribution per analytical point"* note) | **OPEN** — repair landed 2026-09-14, closure not claimed |
| OF-46 | 2026-09-14 | **The printed open-fork inventory can silently omit open rows, because it detects them by MARKDOWN STYLING rather than by ledger structure.** `scripts/print_ledgers.py` selects a row with the literal test `if "**OPEN**" not in line: continue` — a substring search over the whole line, not a read of the status CELL. So a row whose status is genuinely open but whose bold spans a qualification — `**OPEN — banked**`, `**OPEN — and ruled (c) on the principle**` — matches nothing and vanishes from the printed inventory, while the SOURCE ledger still carries it, unchanged and correct. **The failure is silent in both directions that matter:** the printer emits no warning, and the row looks perfectly normal in `open_forks.md`, so the omission is visible only to someone who counts. Four rows are affected today. **OF-33 was hidden this way from 2026-09-13 until [#300](https://github.com/datumwise/columna/pull/300)**, introduced by the OF-33 narrowing itself ([#289](https://github.com/datumwise/columna/pull/289), `a9fce18`) — a row edited to record a ruling disappeared from the inventory as a consequence of recording it. **OF-29, OF-31 and OF-32 are hidden right now**, and were left untouched deliberately so this row could be written against the live state rather than against a tree already normalized to make the printer pass. The mechanism is printed into every `docs` CI run and is named in `CLAUDE.md` as one of the two standing ledgers' presentation path, so the omission reaches every reader who takes the printed inventory as the inventory. | **Rowed, NOT repaired, and the hidden rows deliberately NOT normalized** (Huayin, 2026-09-14). One instance was corrected in #300 — OF-33, because it was a regression introduced by an earlier unit of this same workstream and was reported rather than swept — and the correction moved bold markers only, leaving the wording identical. The other three stand as evidence. | (a) parse the status CELL structurally — split the row on unescaped pipes, take the last cell, and test whether its leading token is OPEN — so styling inside the cell cannot change selection; (b) make the SOURCE format stricter: a machine-readable status token in its own column, with the prose qualification moved beside it, so the printer has a field to read rather than a sentence to interpret; (c) keep the substring test and add a lint that refuses a status cell containing OPEN without matching the literal, turning the silent omission into a loud one. | **The ruling is whether the PRINTER should read structure or the LEDGER should carry a field, and they are different bets about where the discipline lives.** (a) is a one-function change and keeps the ledger a document a human writes freely; its weakness is that it makes the printer cleverer about prose, which is the class of thing that goes subtly wrong again. (b) moves the guarantee into the format and is the only option under which a hidden row becomes impossible rather than merely unlikely — at the cost of editing every existing row and of a format that must then be enforced. (c) is the cheapest honest option and the only one that fixes nothing: it converts an invisible failure into a visible one, which for a mechanism whose entire job is presenting an inventory may be sufficient. What is not open is the finding underneath: **a derived inventory that omits rows without saying so is the same defect class as OF-37**, where the describe-consumer checklist drifted from what it enumerated. Both are mechanisms trusted to be complete that had no way to report being incomplete. | discovered while rowing OF-45, 2026-09-14; `scripts/print_ledgers.py` (the `"**OPEN**" not in line` selection); `specs/open_forks.md` rows OF-29, OF-31, OF-32 (hidden now) and OF-33 (hidden 2026-09-13 → 2026-09-14, [#289](https://github.com/datumwise/columna/pull/289) → [#300](https://github.com/datumwise/columna/pull/300)); `CLAUDE.md` (the two standing ledgers, printed by this script); OF-37 (the same defect class, one mechanism over) | **OPEN** |
| OF-47 | 2026-09-14 | **The ratified realization-v2 conformance claim is not true for `continuation_operator`: on the constructed path the claim is never read at all, and on the primitive path a null claim is exempted from an ESTABLISHED continuation law.** `continuation_operator` is a field of the frozen v2 realization shape (`core_realization_profile_v2_freeze.md` §1) and is loaded, defaulted and carried like its twin `formation_operator` (`realization.py:91`, `:145`, `:245`, `:252`). It is read in **exactly one place in the repository** — `compile_v2.py:437`, `if real.continuation_operator not in (None, op)` — and that site sits inside `if cont.standing == R.ESTABLISHED`, on the PRIMITIVE branch only. Three consequences follow, and all three were **measured on the shipped compiler** rather than reasoned about. (i) **Constructed claims are observationally ignored.** `min(revenue@sale_at)`'s realization was given `continuation_operator: "sum"` — a claim contradicting its own ENTAILED C8 of MIN — and `count(revenue@sale_at)`'s was given `"wildly_wrong"`; both compiled, and each produced an image **byte-identical to the baseline** (`sha 1ebc472e`, 310 bytes, unchanged in all four constructed mutations). CHECK 4 (`compile_v2.py:473-475`) looks like the constructed continuation check and is not one: it compares the governed `ccont` against `op` **derived from the formation law**, so it checks Core's mechanics against governed law and never the realization's claim against either. (ii) **A null claim is exempted where the law is ESTABLISHED.** `revenue` has C8 ESTABLISHED = SUM; removing `continuation_operator` entirely, and setting it to `null`, both compiled byte-identically. A *wrong non-null* claim on that same family **does** refuse — so the field is checked only in the one direction that a careless mapping is least likely to get wrong. (iii) **Where C8 is not ESTABLISHED the claim is unread by construction**, the guard never running, so a realization may name a continuation operator for a family whose governing law establishes none and nothing objects. **The asymmetry with `formation_operator` is the whole finding.** That field has a positive control — `test_a_realization_without_a_delivery_claim_refuses` — and CHECK 3 refuses a missing claim in terms (*"makes no delivery claim"*). `continuation_operator` appears in the test tree **only as fixture data** (`fixtures_v2/lighthouse.py:141`, `test_producer_artifact_v2.py:56`); **no control anywhere asserts any behaviour of it**, which is why a field that is absorbed rather than checked could sit inside a ratified conformance claim without the ratification being false on its face. **AND IT IS ABSENT FROM THE PLATFORM PROFILE ENTIRELY** — `continuation_operator` has zero occurrences in `columna-platform`, so the second profile does not under-check the field, it has no continuation conformance at all. This is an **R0/conformance defect, not missing coverage**: what is wrong is not that a case is untested but that a ratified statement about what the compiler checks does not describe what it checks. | **NOTHING SHIPPED, AND DELIBERATELY NOT REPAIRED IN THIS PR** (Huayin, 2026-09-14): *"Do not repair in the ledger PR."* The behaviour described above is the behaviour on `main` at `e686d41`. The ruling that governs the repair was issued with this instruction and is recorded in the recommendation column; the repair itself, its controls, and the K0v2/Platform reconciliation are a separate landing. | (a) treat the null exemption as intended — a realization that declines to claim a continuation is making no claim, and silence is lawful; (b) require a claim wherever C8 is ESTABLISHED and require its absence wherever C8 is not, on both the primitive and the constructed path, with entailed C8 carrying the same obligation as declared C8; (c) require a claim only on the primitive path, on the theory that a construction's continuation is entailed and therefore not the mapping's to assert; (d) delete the field from the v2 shape as unreadable decoration, and let the formation claim carry the whole delivery assertion. | **(b), and it is RULED** (Huayin, 2026-09-14): *"A realization null means no realization assertion is made. It is never a positive claim that the governed fact is 'none.'"* — with C8 ESTABLISHED requiring a matching named operator, C8 EXPLICIT_NONE and C8 UNESTABLISHED requiring the claim be null/absent, *"the rule applies equally to primitive and constructed families, including entailed C8"*, and the general null interpretation extending to optional realization fields unless a field's ratified contract assigns another meaning. The row therefore does not exist to ask **which** rule; it exists to record that **the ratified claim was untrue before the rule**, which is the thing a conformance ledger must not lose when the repair lands and makes it true. Two things the repair must not be allowed to do quietly: **K0v2 and the Platform profile must be repaired together** — a field checked in one profile and absent from the other is the same silent divergence one layer up — and the repair needs **mutation controls**, because every mutation above compiles today and the only evidence that a future refactor has not re-absorbed the field is a test that mutates the claim and demands the refusal. Note for whoever lands it: `_check_continuation_conformance` is correctly named for what it does (engine mechanics vs governed law) and is **not** the check this row asks for; adding the realization-claim check inside it would merge two different jurisdictions into one function. | continuation reconnaissance, 2026-09-14; the single read site: `columna-core/src/columna_core/compiler/compile_v2.py:437`; the constructed path that never reads it: `compile_v2.py:459-485` (CHECK 4 at `:473-475`); the shape: `columna-core/src/columna_core/compiler/realization.py:91,145,245,252` and `docs/architecture/core_realization_profile_v2_freeze.md` §1; the controlled twin: `columna-core/tests/test_governed_v2.py:486` (`test_a_realization_without_a_delivery_claim_refuses`); the absence of any control: `continuation_operator` in `columna-core/tests/` occurs only at `fixtures_v2/lighthouse.py:141` and `test_producer_artifact_v2.py:56`; the absence from the second profile: zero occurrences in `packages/columna-platform/`; mutation measurements reproduced on `main` at `e686d41` | **OPEN** — repair landed 2026-09-14, closure not claimed|
| OF-48 | 2026-09-14 | **The material path establishes WHAT a delivered value is and never WHERE it is: the anchor's governed coordinate TYPE and coordinate NULLITY are read by nothing, so the analytical point a served row claims to stand at is not established.** The value side of admission is careful in exactly the way the coordinate side is not. CHECK 1 resolves the governed `value_domain`, refuses an unrecognised domain rather than waving it through (*"we did not recognise it" must not read as "it is fine"*), refuses a float carrier, refuses a non-decimal carrier, and refuses outside the measured envelope. CHECK 4 — the only check that looks at coordinates at all (`admission.py:203-221`) — is **exact set equality on NAMES**, in both directions, and stops there. The publication declares `sale_at{store: text, day: date}`; the `type` key is read by **nothing on the material path**, and both sites that read the anchor declaration drop it in the same expression: `serving.declared_components` returns `frozenset(c.get("name") …)` (`serving.py:144-151`) and `movement._declared_components` returns `tuple(c["name"] …)` (`movement.py:93-102`). The anchor-component realization carries no type claim either — it names only `connection/schema/table/column` — so the governed type is the **sole** authority for a coordinate's type and it is consulted nowhere. **MEASURED ON `main` AT `e686d41`, THROUGH THE REAL PROVIDER AND THE REAL PUBLIC WIRE** (not against admission in isolation), with only the deployment's material column swapped: a governed `store: text` coordinate delivered as `int32` serves — `outcome: serve`, `status: served`, four rows, `store=1` as a Python `int` on the wire; a governed `day: date` delivered as `string` serves, with `day='2026-01-01'` as a Python `str` where the faithful run carries `datetime.date(2026,1,1)`; and a **NULL coordinate serves** — `store=None` under an anchor that declares `store`, with no disclosure of any kind. **CHECK 5 IS NOT THE MISSING CHECK AND IS WORTH NAMING SO, BECAUSE IT LOOKS LIKE IT.** It keys analytical points on coordinate VALUES (`to_pylist()`, `admission.py:231-242`) and so it *does* touch the material — but a null is a perfectly good key, `(None, date(2026,1,1))`, distinct from every other, so it passes; and two nulls at one day would be refused as *repeated contributions*, which is the check firing on the wrong property for the wrong reason. **WHAT THIS IS, IN THE VOCABULARY THE REPOSITORY ALREADY USES.** CHECK 4's own comment states the standard — *"a value retained at coordinates the publication does not declare is retained at an analytical point nobody governed"* — and then enforces only half of it: a coordinate whose NAME is undeclared is refused, a coordinate whose governed TYPE is contradicted or whose value is ABSENT is not. An analytical point is not partially specified because Arrow can carry a validity bitmap in a coordinate column; `store=None` is not a point, and a value retained at it is retained nowhere. This is **failure to establish the analytical point represented by delivered material** — the same class OF-45 opened (a fact checked between metadata layers, or not at all, rather than against material), now on the coordinate axis, which is the axis that decides what the value ANSWERS. | **NOTHING SHIPPED, AND DELIBERATELY NOT REPAIRED IN THIS PR** (Huayin, 2026-09-14): *"Do not repair in the ledger PR."* The behaviour above is the behaviour on `main` at `e686d41`. The coordinate envelope ruled for CAP v1 (recommendation column) is the intended repair and lands with that profile, not here. | (a) require the governed coordinate type through a CLOSED mapping — governed type → exactly one Arrow type — refusing an unknown governed type and a mismatched Arrow type alike; (b) require it through an OPEN/coercible mapping, accepting any Arrow type from which the governed type is recoverable (a `date` from a `string` that parses); (c) require only NON-NULLITY and leave the type to the realization's claim, adding a type slot to the anchor-component realization; (d) leave both to the source adapter, on the theory that a driver that delivers an `int32` for a governed `text` column has already misrealized and admission should not re-litigate it. | **(a) plus mandatory coordinate non-nullity, and it is RULED for CAP v1** (Huayin, 2026-09-14): *"Coordinate types are governed facts, so include them"* — `governed text → Arrow string`, `governed date → Arrow date32[day]`, **exact closed mapping**, unknown governed coordinate type or mismatched Arrow type refuses, and *"Coordinate NULL → refuse. An analytical point cannot be partially specified merely because Arrow can carry a validity bitmap there."* **(b) is the one to record as rejected rather than unconsidered**: a coercible mapping would put a parse — a semantic decision about what `'2026-01-01'` denotes in an unstated calendar and locale — inside admission, which is the profile deciding a governed meaning, the exact substitution CHECK 2 refuses on the value axis. **(d) is rejected for the reason the study's own conclusion gives**: *successful transport does not establish admissibility*; a source adapter reports what it received and admission is the only place that knows what was governed. Two notes for whoever lands it. K0v2 already has a closed governed-domain→dtype map (`compile_v2.py:89-92`, `_DOMAIN_TO_DTYPE`) covering `text` and `date` among others; the Platform's coordinate map should be **stated separately and narrowly** rather than imported from it, because K0v2's map is about a family's VALUE domain in a different execution profile and a shared table would silently tie two profiles' envelopes together — the divergence hazard OF-47 names, in the other direction. And the repair belongs at **admission, not at `read_anchored`**: `source.py` renames physical columns to governed names and is the last place that still knows the physical name, which makes it tempting and wrong — a check that lives in the source adapter is a check a different adapter can omit. | continuation/admission recon, 2026-09-14; the half-check: `columna-platform/src/columna_platform/admission.py:203-221` (CHECK 4) and `:231-242` (CHECK 5); the two sites that drop the governed type: `columna-platform/src/columna_platform/serving.py:144-151`, `columna-platform/src/columna_platform/movement.py:93-102`; the governed fact they drop: `columna-core/tests/fixtures_v2/lighthouse-v2-publication.json` (`sale_at` components `{store: text, day: date}`); the careful twin on the value axis: `admission.py` CHECK 1; the class this belongs to: OF-45; measurements reproduced through `PlatformExecutionProvider` + `wire_frame` on `main` at `e686d41` | **OPEN** — repair landed 2026-09-14, closure not claimed|
| OF-49 | 2026-09-14 | **Admission refuses every `decimal128(38,0)` carrier on a stated justification that is false: the measured loss tracks the VALUE's digit count, not the type.** `admission.py` CHECK 1 carries a type-wide refusal — `if t.precision == DECIMAL128_MAX_PRECISION and t.scale == 0` — whose comment states the reason in terms: *"THE FOURTH HOP, MEASURED. Exact in Arrow, lost on the in-process conversion, and nothing raises in between"*, and whose refusal text says the carrier *"is exact in Arrow but is not faithfully carried across the in-process conversion; the envelope is four hops, and this one fails at the fourth"*. Its evidence is one row of the admission-fidelity study: DuckDB `HUGEINT` → `decimal128(38, 0)` carrying `170141183460469231731687303715884105727`, which Polars renders as `1.7014118346046923173168730371588410573E+38`. **That value is THIRTY-NINE digits — out of range for the `decimal128(38, 0)` it is declared as** — and the study measured no other value of that type, so the row cannot distinguish "this type is lossy" from "this value is out of range for its type". **RE-MEASURED, AND IT IS THE VALUE** (`run_3_errata_checks.txt`, errata `[E11]`, landed 2026-09-14): a `HUGEINT` of 38 nines and a `DECIMAL(38,0)` of 38 nines both arrive as `decimal128(38, 0)` and both **round-trip exactly** through `pl.from_arrow`; three distinct **39-digit** values — int128 max, int128 max − 1, and 10³⁸ — all arrive typed `decimal128(38, 0)` and **all** lose digits at the fourth hop. From the other side, pyarrow will not construct a 39-digit `decimal128(38,0)` in-process at all (`ArrowInvalid: Decimal type with precision 39 does not fit into precision inferred from first array element: 38`), which is the same fact approached from the opposite direction and is already recorded, half-seen, in `carrier.py`'s `over_envelope()` docstring — it notes the deviation and reads it as *"a small independent confirmation that the loss is real and driver-mediated"* without noticing that driver-mediated **out-of-range emission** is the whole mechanism. **SO THE REFUSAL IS BROADER THAN ITS EVIDENCE AND ITS STATED JUSTIFICATION IS FALSE.** A conforming 38-digit `(38,0)` carrier is refused today with a sentence asserting a loss that does not occur for it. Note the two halves fail differently and both matter: the refusal may still be the right *decision* — nothing here says `(38,0)` must be admitted — but the *reason recorded in the code* is not true of the thing it refuses, and a reason that is false is worse than a reason that is merely conservative, because the next reader will reason from it. **NON-BLOCKING, AND WHY THAT IS RECORDED RATHER THAN LEFT IMPLICIT:** CAP v1 admits `decimal128(18, 4)` and nothing else, so `(38,0)` is outside the admitted profile either way and no ingress waits on this row. It is rowed because the justification will outlive the refusal — it is the sentence a future amendment to the value envelope would consult. | **NOTHING CHANGED, AND DELIBERATELY NOT REPAIRED INCIDENTALLY** (Huayin, 2026-09-14): *"Do not repair it incidentally."* The refusal on `main` is unmodified. The errata revision corrects the **study's** claim (`[E11]`) and explicitly declines to touch the code; CAP v1 subsumes the refusal by admitting only `(18,4)` and explicitly declines to restate its reason. Both deferred to this row on purpose, so the decision is made once and on the record rather than twice in passing. | (a) **narrow** the rule to what the evidence supports — refuse an out-of-precision VALUE rather than the `(38,0)` type — which is a per-value check and an O(n) pass (CAP v1 §V3, itself deferred); (b) **keep** the type-wide refusal but restate its authority as an explicit conservative profile choice (*"CAP does not admit `(38,0)`"*), abandoning the false empirical claim; (c) **delete** it as redundant, since CAP v1's positive envelope already admits only `(18,4)` and a second refusal of a shape that is not admitted is dead law; (d) keep both — a narrow type refusal AND a value-range check — on the theory that they guard different failures. | **The question is which AUTHORITY the refusal stands on, not whether `(38,0)` should be admitted** — and that separation is the whole reason this is a row rather than a patch. (b) and (c) are the live candidates and they differ in what happens at the NEXT amendment: (c) leaves nothing behind, so an amendment that widens the envelope inherits no warning and would have to rediscover the out-of-range hazard; (b) leaves a stated profile choice that a future author must positively overturn, which is the cheaper failure mode. (a) is the empirically correct rule and is the one CAP v1 §V3 defers — its cost is real now and the hazard is not reachable inside a `(18,4)`-only envelope, so adopting it here would be paying for a guard against material this profile cannot receive. (d) is belt-and-braces and would leave the false sentence in place unless (b)'s restatement rides along, which is the failure this row exists to stop. **Recommendation: (b), with (a) named as a mandatory precondition of any amendment that admits a decimal shape wider than `(18,4)`** — so the hazard is carried forward as an obligation instead of as a comment. Whichever is chosen, the code's stated reason must stop asserting a measured loss for values that do not suffer one. | errata `[E11]`, 2026-09-14; the rule: `columna-platform/src/columna_platform/admission.py` CHECK 1 (`t.precision == DECIMAL128_MAX_PRECISION and t.scale == 0`); the constant and its citation: `columna-platform/src/columna_platform/carrier.py` (`DECIMAL128_MAX_PRECISION`, and `over_envelope()`'s recorded one-digit deviation); the original row: `docs/architecture/admission_fidelity_study_v0_1.md` §3 (`HUGEINT` / int128) and §6 collapse point 3; the re-measurement: `docs/architecture/admission_fidelity_v0_1/run_3_errata_checks.txt` and `probe_3_errata_checks.py`; the profile that makes it non-blocking: `docs/architecture/carrier_admission_profile_v1_candidate.md` §2 | **OPEN** |
| OF-50 | 2026-09-14 | **CAP v1's value envelope was RATIFIED and never ENFORCED, so material outside the ratified profile served through a real driver.** CAP v1 (ruled 2026-09-14) admits **`decimal128(18, 4)`, exactly that and nothing else** — `docs/architecture/carrier_admission_profile_v1_candidate.md` §2, whose own subsumption table (§7) records that under it the `precision > 38` bound and the `(38,0)` refusal are *no longer load-bearing*. The shipped code on `main` (`b67c90e`) implements a **different rule**: `admission.py` CHECK 1 refuses `precision > 38`, refuses the `(38,0)` special case, and **admits every other decimal shape** — which is the BROAD envelope whose ratification was WITHDRAWN on four evidential grounds recorded in the profile itself (the excluded case excluded on a misread discriminator, study `[E11]`/OF-49; the widest supporting shapes never crossed hop four, `[E4]`; nothing above precision 38 ever run, `[E3]`; the interior of the range never measured on any crossing). **So this is not a missing check. A check was present, and it enforced a withdrawn rule.** **MEASURED, NOT ARGUED** (2026-09-14): with `main`'s shipped CHECK 1 restored over #309's test tree and run against the real vendored ADBC surface, DuckDB `DECIMAL(9,2)` → `decimal128(9, 2)` and `DECIMAL(38,9)` → `decimal128(38, 9)` both cross into Arrow, are ADMITTED, and reach the public wire with `outcome: serve` — two shapes outside the ratified profile, served, with no disclosure. Under #309's repair the same two parametrizations refuse. **WHY IT SURVIVED RATIFICATION DAY:** every committed carrier fixture in Platform is `decimal128(18, 4)` (`tests/conftest.py:156`, `test_material_execution.py:263`, `test_material_formation.py:319`), the one shape both rules admit identically, so the whole suite passed byte-identically under the ratified rule and the withdrawn one. Nothing in the tree compares the admitted set to the ratified profile, so the divergence was not merely unobserved — it was **unobservable from inside the tests**, and it was found by accident while building something else. **THE CLASS, WHICH IS WHY THIS IS A ROW AND NOT JUST A FIXED BUG: ratification is not enforcement, and this house has no mechanism that makes it so.** A ratified profile that no code reads is a document; the period between ratifying CAP v1 and enforcing it was a period in which the repository's stated admission authority and its actual admission behaviour were two different rules, and the only reason that period was short is that an unrelated implementation walked into it. | **REPAIRED IN #309, AND CLOSURE IS NOT CLAIMED BY THIS ROW.** [#309](https://github.com/datumwise/columna/pull/309) replaces CHECK 1's envelope with a single equality against `CAP_V1_DECIMAL = (18, 4)`; the `precision > 38` bound and the `(38,0)` branch are DELETED as subsumed per §7, not kept alongside. Negative control: `test_a_decimal_outside_the_cap_v1_envelope_is_refused`, parametrized at `(9,2)` and `(38,9)`, asserting the shape arrives INTACT and is refused **by profile choice, not by loss**. Ruled (Huayin, 2026-09-14): keep the correction; **do not restore the `(38,0)` branch** — CAP v1 is deliberately narrow and anything outside its positive envelope already refuses; OF-49 stays open for independent adjudication, and an obsolete implementation is not preserved merely because its historical row is open. What #309 repairs is ONE profile clause at ONE point; the row's question is the class, so the finding unit does not claim closure. | (a) **leave it to review** — each ratified clause hand-checked whenever someone touches admission (the status quo, which is what just failed); (b) **a conformance control** that names the ratified envelope literally and fails if the code's admitted set differs from it, so widening the code forces an edit at the place the amendment obligation is written; (c) **make the profile machine-readable** — the envelope as data beside `specs/`, imported by admission, so the document IS the authority and the two cannot diverge; (d) **treat #309's repair as sufficient** and close. | **(b).** (d) is rejected on its own history: the repair is correct and was found by accident, and a class whose only detector is coincidence is undetected. (c) is the tempting one and it is **worse than it looks** — it closes the conformance gap by making the envelope a config value, which drops the cost of widening it to a one-line data edit, and CAP v1 §8 requires an amendment to carry *a measurement through the complete path plus a ratification*. A profile whose force comes from being ratified must not be amendable by editing a table nobody reviews as law. (b) keeps the envelope in code where a change is a code change, and adds the one thing missing: a test that fails when the code and the ratified document stop saying the same thing. Scope it to the profile's §7 subsumption table — the admitted value shape, and the refusals declared no-longer-load-bearing — not to the whole document. **And record the general obligation with it: a ratified profile lands with the control that enforces it, in the same PR, or it has not landed.** | CAP v1 §2, §7, §8: `docs/architecture/carrier_admission_profile_v1_candidate.md`; the unenforced rule: `packages/columna-platform/src/columna_platform/admission.py` CHECK 1 at `b67c90e`; the repair and its control: [#309](https://github.com/datumwise/columna/pull/309) (`eabde68`), `admission.py` `CAP_V1_DECIMAL`, `packages/columna-adbc/tests/test_duckdb_adbc_ingress.py::test_a_decimal_outside_the_cap_v1_envelope_is_refused`; the measurement (2026-09-14): `main`'s CHECK 1 restored over #309's tree, both parametrizations reach `outcome: serve`; the fixture that hid it: `packages/columna-platform/tests/conftest.py:156`; the withdrawn envelope's grounds: `docs/architecture/admission_fidelity_study_v0_1.md` errata `[E3]`, `[E4]`, `[E11]` | **OPEN** |
| OF-51 | 2026-09-14 | **The dependency-cap guard does not recognise a cap written as a SINGLE CLAUSE, because it never separates the package name from the specifier — so the vocabulary the guard documents is not the vocabulary it implements.** `scripts/assert_dep_caps.py` states *"WHAT COUNTS AS A CAP: `<`, `<=`, `==`, or `~=`"*. `has_cap()` strips the environment marker and extras, then splits on `,` and tests `clause.startswith("<") or clause.startswith("==") or clause.startswith("~=")`. **That test can only pass on a clause that does not carry the package name** — i.e. on the second and later clauses of a comma-separated requirement. The first clause of every requirement begins with the distribution name, so the operator is never at position 0. **Measured** (2026-09-14): `package==1.5.5`, `package<=2`, `package~=1.4`, `package<1.0` all return `False`; `package>=1.5,<2` returns `True`. The guard is correct exactly for the spelling the repository happened to use everywhere, and wrong for every single-clause spelling — including the exact pin its own docstring names first. **THE DIRECTION OF THE ERROR, STATED PRECISELY, BECAUSE THIS ROW WILL BE READ AS AUTHORITY:** `has_cap` errs **only in the `False` direction**, so it is a false negative *on the question it answers* ("does this requirement carry a ceiling?") and **no uncapped dependency has ever passed the gate because of it**. The CI-visible consequence is a spurious FAILURE, not a silent pass. What is genuinely silent is the period before anyone writes such a pin: no dependency in the tree used a single-clause cap, so the defect had never been exercised, while the docstring — the thing an author consults to learn what counts — asserted a vocabulary the code does not honour. **A guard is trusted for what it says as much as for what it runs, and here those were different.** **THE SECOND COST, WHICH OUTLIVES THE CI FAILURE.** It surfaced in [#309](https://github.com/datumwise/columna/pull/309), which needs an EXACT pin on `duckdb`: the vendored `adbc_driver_duckdb` sub-package has no distribution, no independent version and no deprecation channel, so a RANGE there would be a compatibility claim nobody made. The guard rejected `duckdb==1.5.5`, so the pin ships as `duckdb>=1.5.5,<=1.5.5` — a degenerate range that IS `==1.5.5` — written that way *solely so the guard can see the ceiling*, with the same workaround at `columna-platform>=0.0.0,<1.0` and `adbc-driver-manager>=1.12.0,<=1.12.0`. The declaration now READS as a range and its real meaning survives only in a comment. **A guard that pushes correct declarations into workaround spellings degrades the metadata it exists to protect** — the next author copies the spelling, not the comment. (The npm half of the guard, `"<" in r` as a substring test over the whole range, has the same shape of looseness and is **not audited by this row**.) | **NOTHING CHANGED. The guard is untouched on `main` and untouched by #309** — ruled (Huayin, 2026-09-14): *"Do not repair it in the ledger PR"*, and do not fold the repair into #309. #309 works around it **at the declaration site and says so in the file**: `packages/columna-adbc/pyproject.toml` records that `has_cap` tests `clause.startswith("==")` after splitting on commas and that the only clause is `duckdb==1.5.5`, which starts with `d`. The guard was deliberately NOT weakened to accommodate the pin. | (a) **parse the requirement** — `packaging.requirements.Requirement(req).specifier`, checking each `Specifier.operator` against the documented set, i.e. use the syntax the docstring already claims to support; (b) **keep the hand-rolled split** and strip the leading distribution name with a PEP 508 name regex before the operator test; (c) **narrow the docstring** to what the code does (*"a ceiling must be its own comma clause"*) and adopt the degenerate-range spelling as house style; (d) do nothing and keep working around it per requirement. | **(a), in a separate bounded PR — not folded into #309** (Huayin, 2026-09-14), **and the controls are the deliverable, not the parser.** For each documented operator form (`==`, `~=`, `<`, `<=`) a single-clause POSITIVE case the guard must recognise; for `>=`, `>`, `!=`, a bare name, and a marker carrying `<` a NEGATIVE case it must still flag; and a MUTATION control that neuters the fix and shows every positive case reverting to a reported offender — the guard's own doctrine is that a guard proved in one direction is not proved. (b) is acceptable if adding `packaging` to the guard's runtime is unwanted; the parser is not what makes this safe, the negative controls are. (c) is rejected: it makes the workaround permanent and invents a spelling requirement PEP 508 does not have, so the repository would be enforcing a private dialect and calling it dependency hygiene. Whichever is chosen, **the docstring and the behaviour must state the same vocabulary — this row exists because they did not.** | `scripts/assert_dep_caps.py` (`has_cap`, `CAP_OPS`, and the docstring's *"WHAT COUNTS AS A CAP"*); the gate: `scripts/gates.toml:147` (`dependency cap guard`, `ci.yml`); the workaround and its recorded reason: `packages/columna-adbc/pyproject.toml` `[project].dependencies`; found while building [#309](https://github.com/datumwise/columna/pull/309) | **OPEN** |

## Authorized work moved

The **authorized-work class** (ruled work tracked to verified completion) lives in
[`doctrine_gaps.md`](doctrine_gaps.md), not here — Huayin's consolidation ruling, 2026-07-25:
**no third ledger**. That file's remit, *"a ruled item leaving scope is a checkpoint event … never a
silent drop"*, is that class verbatim; minting a second home for it was how the fossil ruling
evaporated in the first place.

**This file keeps genuine undecided forks only** — code ahead of doctrine, awaiting a ruling.

## Log
- **OF-6 opened 2026-07-16** (CP-2 artifact 3). Draft persistence: the in-memory Draft needs a
  cross-session serialization once the A4 human turns span sessions — which the very next step (a real
  provider / interactive init) triggers. Opened now per Huayin: "the next step will want it" is a ledger
  row, not a prophecy.
- **OF-5 opened 2026-07-16** (CP-1 increment 6). The declared spine-grid (a level's domain source) is
  the single missing object behind both single-column events fill and the spine/product completeness
  oracle. Per Huayin: internal-contiguity is refutable from the data's own testimony in principle, but
  the connector domain-read (min/max/distinct on the ordered axis) it needs is exactly this grid, so
  the live CONTRADICTED channel rides here; absence is juxtaposition-scoped and BASIS mints UNTESTABLE
  meanwhile (serving always follows the declaration).
- **OF-4 opened 2026-07-16** (CP-1 §2c). The query-side ON-UNIVERSE mechanism (server `universe` arg,
  wire universe-apply, agent clarify-pick) is dormant after §2c retired its last emitter
  (`co_anchor_ambiguous`). Per Huayin's scope-split ruling, the core laws land now and the server-arg
  removal + agent pick-flow redesign are a pinned deferral. `test_clarify_relayed_not_auto_picked`
  asserts the load-bearing relay-and-never-auto-pick; the mechanical pick round-trip moves with this row.
- **OF-1, OF-2 opened 2026-07-14.** Transferred verbatim from PR #18's description into durable form,
  per Huayin's ruling (2026-07-14): "a merged PR's body is neither ledger nor queue… every fork
  surfaced in a PR gets a row [here] before the PR merges." Both were shipped in WP-B.1 (merge
  `a074319`) using the closest-fitting existing codes (no minting); the codes stand until Huayin rules.
- **OF-1 RULED (b) & CLOSED 2026-07-14** (Huayin). Minted reason `input_anchor_ambiguous`
  (CLARIFY/AMBIGUOUS), sibling to `co_anchor_ambiguous`; `ambiguous_grain` gloss left single-meaning.
  Standing rule recorded at `disclosure.py`'s `REASON_OUTCOME`: **one reason per contested dimension**.
  Landed with its test (`test_input_anchor_ambiguous_is_a_distinct_clarify_reason`).
- **OF-2 RULED (a) & CLOSED 2026-07-14** (Huayin). Immaterial `provenance` note ratified as shipped;
  no code change. Boundary recorded here and at the `_resolve_inline_reduction` docstring: material
  `input_anchor` is for imported/defaulted anchor choices; an explicit pin owes only the immaterial
  note, because the wire's reader may not be the asker.
- **OF-3 opened 2026-07-16** (CP-1, on-ramp/Explorer tier-2 WP, [PR #35](https://github.com/datumwise/columna/pull/35)).
  The ASSERT invariant-form ships its full data channel; the **row-form** is recorded UNTESTABLE (no
  base-row scan yet). Ledgered as a durable row per Huayin's rider (2026-07-16): "a later increment is
  where scoped items go to drift." Gates nothing; struck when the row-form's data channel is built or
  ruled unnecessary. Referenced at `adjudication.py::_prove_assert` (row branch).

- **OF-7 opened 2026-07-17** (CP-3, describe + Explorer). The Explorer ships as a portable component in
  `apps/website/` (the `/explorer` demo-manifold instance); the package-served deployment (columna-server
  offering it against a live manifold) is the recorded near-future path, a later WP. Gates nothing.
- **OF-8 opened 2026-07-17** (CP-3). An author-facing provenance surface (FROM/VIA) is a conceivable future
  Explorer layer whose data could never come from describe (the §2b insulation guarantee) — it would need
  its own governed source. Rowed, NOT designed (Huayin: 'row it, don't design it'). C-3 stays describe-only.
- **OF-9 opened 2026-07-17** (CP-3 C-2). Drop-the-qualifier ships as the insulation guarantee: no
  STRUCTURAL physical identifier crosses describe (no table names, no qualified table.column), while a
  bare predicate attribute name renders as authored. The full fix — declared logical names for predicate
  terms — is a definition-language extension (a new authoring surface, init implications), its own WP, not
  improvised before a freeze. The standing test asserts exactly the structural guarantee; it tightens to
  full verification when OF-9 lands. (Huayin: a test that asserts current behavior blesses current bugs —
  the no-physical test is standing and STRUCTURAL, which is why it caught the stores.opened_date leak.)
- **OF-10 opened 2026-07-17** (CP-3, S-1 superseded). The §2c definition-time-population mechanism
  lost its named customer (sell_through_rate killed) — rowed anyway; rows record designs, not sponsors.
- **OF-11 opened 2026-07-17** (CP-3b). The hosted access point: the demo Manifold as a live
  socket with an HTTP plug (direct FrameQL, path e) + an MCP plug (agents) on one API. Rowed,
  not built — post-launch WP; the wire is already the API contract, so the lift is transport + ops.
- **OF-12 opened 2026-07-17** (WP-FrameQL envelope, POST-FLIP beat — Huayin, at the 0.9.0 release-notes
  gate). A `frameql` **grammar-version field advertised in `describe`**, so any external agent can detect
  which grammar a server speaks (the terse fragment vs the 0.9.0 envelope). Additive, transition-friendly;
  its own beat. Explicitly does NOT reopen #49 (the surface-migration increment). The package semver + the
  dated `parse_frameql` tombstone already carry the break; this is the machine-readable advertisement for
  a heterogeneous fleet.
  **ANNOTATED 2026-09-11 (Frame-QL 1.0 expression unit).** One of the two grammars this row exists to
  tell apart no longer ships. The terse fragment's parser left the public API with the 1.0 retirement
  (`columna_core.__all__`, the `columna_server.frameql` re-export); it is quarantined private as
  `_parse_retired_fragment`, kept for reading archived text and reachable by no surface. So a server
  built from this tree speaks exactly one grammar, and the fleet OF-12 was written for is a fleet of
  *older releases* — the field's job narrows from "which of two grammars do I speak" to "which
  RELEASE's grammar do I speak", which is a version advertisement, not a grammar one. That is a
  smaller and more ordinary thing to build, and it is worth re-deciding whether the package semver
  already carries it. Rowed, not re-scoped: this is evidence for the next ruling on OF-12, not a ruling.

- **OF-13 opened 2026-07-18** (Cascadia case-demo recapture, POST-FLIP fork — Huayin). **Coordinate-value
  predicates in `WHERE`.** The recapture's manager transcript wanted `SELECT revenue, orders AT
  {cal.quarter} WHERE region = west` — slice to one region's value — and it does NOT resolve in this build
  (`unsupported` / BinderException; a query-level `WHERE` cannot filter on a dimension coordinate value,
  base OR rollup). Working construction today: anchor at `{region, cal.quarter}` and read the row (accepted
  for the transcript — honest, realistic agent behavior; ch3's prose shows only the NL answer, no edit).
  But "west only" is bread-and-butter day-one slicing a human WILL type — the case demo exposed a real
  expressiveness gap. Rowed as its own ruled increment, post-flip; not this WP.
  **EVIDENCE UPGRADED (2026-07-18, ch3 take-2):** the gap is not hypothetical — the LIVE query agent hit
  it TWICE in one recording (manager `WHERE region = 'west' AND cal.quarter = '2025-Q4'`; new-hire
  `WHERE cal.year = 2024`), both BinderException, both cascading to a suppressed "couldn't read" reply.
  The WHERE-a-coordinate instinct is UNIVERSAL — it is the SQL reflex; every MCP stranger's agent will
  reach for it. This is now the **leading candidate for the first post-launch language increment.** The
  interim measure is the agent-prompt law **SCOPE BY ANCHOR, NOT WHERE** (ratified 2026-07-18); **when
  OF-13 lands, that prompt law amends** (the anchor-and-read workaround retires as the language grows the
  native filter). Linkage recorded so the two move together.
  **EVIDENCE UPGRADED AGAIN (2026-07-18, ch3 take-3):** with the anchor-and-read law in place, the agent
  stopped failing — but the anchor-and-read serves the WHOLE frame (32 region×quarter rows); the answer
  (west/2025Q4) is present in it but frame-weighted, not isolated. So even the workaround does not yield
  a clean isolated answer. **OF-13 is the PRIMARY fix (language-first doctrine):** a native coordinate
  filter fixes EVERY client — including strangers' agents who will never read our prompt — and it is the
  only fix that yields a clean answer at the language layer. Confirmed the leading candidate for the
  first post-launch language increment. Post-flip.

- **OF-15 opened 2026-07-18** (Cascadia recapture take-3, SECONDARY fork to OF-13 — Huayin). **Agent
  read-then-summarize (a non-terminal `query`).** Today `query` is the agent loop's TERMINAL act: it
  serves the whole frame and ends the turn, so the agent cannot read a served result and report only the
  matching row(s) in prose. A non-terminal read-then-summarize step would let our agent isolate
  "west/2025Q4 = $31,468.78, 402 orders" from the served frame. This is SECONDARY to OF-13: it benefits
  OUR agent only (a stranger's agent gets nothing from it), whereas OF-13's native filter fixes every
  client at the language layer. Consider only if OF-13's design proves slow. Post-flip.

- **OF-14 opened 2026-07-18** (Cascadia recapture take-1, STANDING FACT — Huayin). **The demo has no
  clock.** The frozen-world demo warehouse (2024-2025) has no notion of "today", so relative-time
  phrases — "last quarter", "this year" — do not resolve; the assistant correctly ASKS rather than
  guesses (recorded in ch3 take-1: the manager's "last quarter" drew a clarify, now folded into the
  story as a two-turn exchange). This is a standing fact of the frozen world, NOT a bug. If a future
  increment ever wants relative-time resolution (a declared "as-of" anchor, a clock), it is a design
  fork — rowed here, not owed.

---

## 0.12 "the triad completes" — forks + rowed items (PR #83: 0.12-triad; MERGED + PUBLISHED 2026-07-24)

- **The five user-facing strings — RATIFIED, SEALED, WIRED BYTE-EXACT** (Huayin 2026-07-24, supersede
  all placeholders): the `primary` / `split` face descriptions (`manifold.cml` → menu + wire); the G4
  chain-guard message and the G5 anchor-law refusal (`engine.py`); the ledger re-sequencing note
  (below). No longer a fork — sealed.
- **ROWED (Huayin v0.4, non-blocking, no action this increment):** `last ORDER category` on the
  degenerate `category_profile` spine answers a coarser-than-base ask with a declared-but-arbitrary
  value (`priority` at the `all` grain = the last category's priority). Acceptable for v1 drivers;
  profile-spine measures eventually want a **base-grain-only family form**. Joins the ledger beside the
  crossed-population door.
- **Acyclicity DAG is VACUOUS in 0.12** (fork/observation): a lawful driver is a spine functionally
  servable at the frontier, so the face-driver graph is edgeless — a cycle is unexpressible until a
  driver can be a *crossed* measure (events-derived, 0.13+). The `_prove_faces_acyclic` machinery ships
  and runs (forward guard); the realizable failure this increment is the driver-must-be-a-spine check
  (an un-frozen events driver refuses — `test_adjudication_fails_on_an_unfrozen_events_driver`).
- **G5 SERVE-half deferred to 0.13** (ruled B, on the record, not drifted): distinct-rides-`.touch`
  (per-member sketch reduce + overlap disclosure) lands beside P1/alignment, where the anchor-exhaustion
  corollary (notes §6.3) is proven, not asserted. 0.12 ships the refuse-half only — the anchor law.
- **/case exhibit copy for E11/E12** comes to the desk POST-BUILD (addendum §8); the recapture captions
  are working placeholders, like E10's.
- **Ledger re-sequencing note — SEALED (public ledger, dated); byte-exact, single line:**
  "2026-07: the RELATE face triad (assign/alloc) completes ahead of OF-13 — shipped-surface coherence and the crossing menu's completion graded ahead; OF-13 remains the leading language increment thereafter, joined by multi-universe alignment (P1). The distinct-crossing serve-half was explicitly deferred to the alignment increment — ruled, not drifted."
  The public-website placement rides the publish deploy.
- **Version track — CONFIRMED (Huayin 2026-07-24):** core → 0.12.0, meta → 0.12.0, server → **0.7.0**
  (its own line; "across" = the release spans all three, the track precedent governs).

## 0.13 "the ASSERT retirement" — the public ledger row (ruling §6; MERGED + PUBLISHED 2026-07-26/27)

- **Ledger renumbering note — SEALED (public ledger, dated); byte-exact, single line:**
  "2026-07: 0.13.0 is the ASSERT retirement — ruled, not drifted: the admission test (everything a trial proves is a precondition of something served) removed the one construct that failed it, plus its cascade. Multi-universe alignment (P1), previously 0.13, renumbers to 0.14 unchanged in scope."
- **0.13.1 — the reconciliation delta, and the doctrine it carries.** *A value below the system's
  declared tolerance is noise, not a finding — reporting it as a finding is FALSE PRECISION*, which is
  a species of confident wrong number. The masthead does not say "no *large* wrong numbers." Within
  tolerance the wire now reports at the resolution its own tolerance warrants; outside tolerance a real
  shortfall keeps its exact value and sign, asserted by test so the guard can never launder a finding.
  Wire change NAMED, not silent; no `contract_version` bump.
- **PROVENANCE of the signed-zero row, kept in full because the ledger keeps honest histories:**
  flagged at the **#85 preview** → ordered into the **0.12.1 cargo** → **never landed** (ordered-but-
  unlanded, pre-heartbeat vintage) → resurfaced as a **flap** during the 0.13.0 confirmation re-record,
  ~20% of runs on an identical package and input → **misdiagnosed twice as a signed zero, once by the
  builder and once by the desk**, neither having looked at the number → **measured** (the raw value
  alternates between exactly `0.0` and ±2⁻³¹, decided by float summation order) → **fixed at the true
  cause**. The prescribed fix `x if x != 0 else 0.0` was implemented and *proven not to work* before the
  real one was written.
- **The epistemic record, both directions:** *readings do not verify each other — and a symptom named
  in a report is still a reading.* The desk ruled on a symptom name rather than a measured value. The
  desk is not exempt from the epistemics it enforces.
- **The structural guard** (`check_generator_determinism.py`): every committed-output generator runs
  twice and must be byte-identical, wired into the deploy. Verified in BOTH directions — provoked
  against the unpatched engine, where it fails closed naming the generator and the differing byte.
  A recorded exhibit changes only by re-recording; this enforces that it does not change by *itself*.

**CLOSED 2026-07-24 — Huayin's word arrived, four-in-one (the ch2 forward-reference sentence, the
Figure 1 caption ratification, the merge, the publish), executed publish-first:** columna-core 0.12.0 ·
columna-server 0.7.0 · columna 0.12.0 live on PyPI (OIDC Trusted Publisher + `assert_pypi_versions`
green; per-version endpoints 200); PR #83 merged to `main` (`f0db0e0`, merge-commit — `v0.12.0` tag in
history); Vercel prod deployed via the shipped-coherent wedge (installed 0.12 from PyPI, regenerated
every exhibit from the published package). Live-verified: Figure 1's interim two-universe filter +
`OMITTED UNIVERSE · INTERIM` ghost + `DISCLOSE · INTERIM` caption band, and the ch2 forward-reference
sentence on /case + llms-full. RED 1 (regen-check `_FINANCE_CML`, purged-EDGE fixture — fails
identically on main, untouched by this PR) holds as the dated post-flip micro-PR. **The 0.12 record is
closed.**

## 0.13.2 "the declared Python floor and ceiling" — the launch-eve packaging row (MERGED + PUBLISHED 2026-07-27)

- **The finding, and how close it came.** A Windows fresh-venv pass on launch eve found `pip install
  columna` on Python 3.14 **not failing but BUILDING** — a C++ source build of `datasketches`, which
  core hard-depends on for HLL. `requires-python` was `">=3.10"` on all three packages with **no upper
  bound**, so pip considered us a match, found no wheel, and fell through to a compiler. Checked
  against PyPI, the gap is wider than the report: datasketches 5.x publishes **zero cp314 wheels on
  any platform** (so the ceiling protects Linux and macOS too) and has **never** published a 32-bit
  Windows wheel at any version. Hence the ratified line: *"Requires Python 3.10–3.13, 64-bit."*
- **THE DOCTRINE:** *fail closed with a named reason beats rare success for whoever happens to own a
  compiler.* An unbounded `requires-python` is not permissiveness — it is an untested claim asserting
  support for every future Python, including ones that did not exist when the claim was written.
- **The second, unlooked-for defect — `demo --play` crashed on Windows.** Found by the new
  windows-latest CI leg **on its first run**, which is the entire argument for the leg. Piping or
  redirecting the demo died with `UnicodeEncodeError: 'charmap' codec can't encode` **on the opening
  line, before a single mood printed**. Python writes to a Windows *console* through the wide-char
  API, so an interactive run looks fine; the moment stdout is a pipe or a file it falls back to the
  locale encoding — cp1252 on default en-US Windows — and our output is legitimately non-ASCII (U+2500
  rules, em-dashes, wire JSON dumped `ensure_ascii=False` so non-ASCII labels serve as themselves).
  **THE FIX BELONGS AT THE STREAM, NOT IN THE TEXT.** Stripping the characters to ASCII would have
  made the demo lie about what the wire carries, and would have left the next non-ASCII value to crash
  somewhere quieter. `columna-server` now declares UTF-8 on stdout/stderr for every subcommand.
  Guarded twice: the Windows leg is the real proof, and `test_demo_play_survives_a_cp1252_stdio_locale`
  reproduces the exact failure on **any** platform via `PYTHONIOENCODING=cp1252` — verified by
  neutering the fix and watching it reproduce the identical Windows traceback on Linux. That test
  asserts the rules and em-dashes are still PRESENT, so it fails if anyone "fixes" this by deleting them.
- **The class guard.** `demo wheel install` now runs **windows-latest / py3.13** beside ubuntu /
  py3.10. The class is *a dependency with platform or version wheel gaps*; it was invisible to a
  Linux-only CI, which is how it survived three releases. Both legs run one assertion file
  (`scripts/assert_demo_play.py`) so they cannot drift into proving different things — and that file
  is ASCII-only on purpose: **a guard that can be broken by the condition it guards against is not a
  guard.** (Its first Windows run printed an em-dash only because cp1252 happens to hold one at 0x97;
  U+2500, four lines earlier in the product's own output, does not. That is exactly where the crash
  landed.)
- **THE WEDGE RACE — ROWED, then fixed by choreography, not code.** The deploy resolved **on attempt
  5 of 5**, the final attempt of its retry budget. It went green with no margin. The cause is ORDER,
  not budget: merging to `main` triggers the deploy while publish is triggered by the release cut that
  necessarily *follows* the merge, so the deploy always races the publish. **The fix is the flip
  choreography the house had already proved twice (0.12.0, 0.13.0): tag the release branch BEFORE
  merging — publish fires, the verbatim-pin gate clears, THEN merge, so the deploy finds packages
  already installable.** Recorded as the standing release order in `docs/RELEASE_ORDER.md`; zero code.
  Belt for a Tuesday emergency: the retry budget widened 5 → 8 (~560s), pre-freeze, one integer.
- **THE VANTAGE-POINT CLAUSE, RE-PROVED FROM THE OTHER DIRECTION.** At 0.13.1 a developer machine
  called the pin installable while the CI runner, on another CDN edge, could not resolve it. At 0.13.2
  the disagreement inverted: `/pypi/columna-core/json` reported 0.13.2 **absent** while `/simple/` —
  the index pip actually resolves from — was already serving it. A checker trusting the JSON would
  have declared a live release broken and rolled it back. So the clause is **symmetric**: the JSON API
  is not a slower mirror of the truth, it is a DIFFERENT VANTAGE POINT that can be stale either way.
  *Verify installability by installing*, with the resolver a consumer actually uses — never by reading
  a metadata endpoint that merely describes the package. A convenient observation is still an observation.
- **THE GUARD PATTERN — FOUR GUARDS, FOUR LIVE FINDINGS, NOT ONE EVER RAN CLEAN.**
  *(Ratified in this wording by Huayin, 2026-07-27, over the rounder "four first-run catches."*
  *Precision about our own claims is this house's brand applied to its own mythology: the round*
  *number would not have survived an auditor, and we are the auditor.)*

  The count was **checked, not assumed** — each guard's introducing commit was traced before the
  line was written, and the trace is kept here so the claim stays auditable:

  | guard | introduced | what it found, and when |
  |---|---|---|
  | `check_generator_determinism.py` (flap detector) | `7e9b613` (0.13.1) | caught `gen_transcript.py` flapping on a later run → **OF-23 rowed** (`95f5fb5`). A real catch after introduction. |
  | `latest.ts` fail-closed release rail | `4922fec` | landed **after** `v0.13.1`, and its own commit pre-seeded every entry through 0.13.1 — so **0.13.2 was the first version it had never seen, and it fired**, refusing the build until someone wrote what the release IS. Strict first-run catch. |
  | windows-latest CI leg | `8392372` (0.13.2) | **caught a real product crash on its first run** — the cp1252 `demo --play` death, fixed in `627d8b1`. Strict first-run catch. |
  | `check_purged_grammar.py` | `483fac1` (2026-07-25) | **a different shape, and the record says so**: born *from* a hand-found fossil sweep and born *carrying* a live ROWED exemption (OF-16). It never had a clean first run to catch anything on, because it was already holding a finding at birth. |

  **So: three strict first-run catches, and a fourth guard that arrived already flagging.** The
  pattern is not "guards catch things eventually." It is that **every guard this house has added
  found a live defect at or immediately after introduction — four for four, none ever clean.**
  That is the argument for adding the next guard *before* it is asked for. It is also the warning
  underneath it: **the defects were already there, and the only variable was whether anything was
  looking.** A guard is not what creates the finding; it is what ends the period of not knowing.

## Occurrence conformance — RULED (Huayin 2026-07-24; rides OF-5)

The three-universe Figure 1 (PR #84) draws two inter-universe edge classes from the DECLARATION
(describe): the shared-grain **"=" identity** edges (store, day, declared identical across transaction
and inventory) and the **product↔category frontier + faces**. Their *conformance* is one adjudication
class — **"occurrence conformance"**: frontier-side `bridge ⊆ members` and identity-seam-side
`occurrences ⊆ members` (per universe, per shared grain). It is **NOT in the shipped adjudication** and
is **blocked on OF-5's unbuilt domain-read** (min/max/distinct, membership); BASIS minting `UNTESTABLE`
is the correct interim truth. **Ruling: a dated micro-PR riding OF-5**, not add-now — it lands when the
domain source is declarable. The figure needs no engine change: it depicts declarations, not conformance.

## OF — `in_core` is overloaded (implementation debt, not semantic authority) — ROWED (Huayin 2026-09-01)

`Operator.in_core` carries at least two different facts under one flag, and reading it as a single
realization axis mismeasures.

* For `rolling_sum` / `rolling_mean` it means **the mechanics do not exist**: nothing executes, in any
  position, and the engine answers a governed `unsupported` [ROADMAP].
* For `mean` it means only that **Core does not serve the average as a declared MEASURE FAMILY
  MEMBER**. The inline form `mean(x @ {a})` executes today; the registry entry exists so
  `(operator × lineage)` law has an address, not to gate execution.

Found during the capability-authority reconciliation (2026-09-01): a first measurement that read the
flag alone reported `mean` as *lagging* a Core Profile it actually meets.

**Ruling: implementation debt, not semantic authority. Do not block Manual work on a refactor.**
`docs/tools/capability_authority.py` keeps conformance/profile measurement independent of the flag —
it disambiguates through `SERIES_REDUCERS`, the declared inline-execution vocabulary — and the
overload is rowed here for later cleanup. The flag must not become the authority for what a profile
or the language requires; that is what `specs/frameql_capabilities.toml` and `specs/profiles/` are
for.
