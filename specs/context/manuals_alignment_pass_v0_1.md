# Manuals alignment pass (v0.1) — the ADR-034/035 worksheet
**Companion to ADR-034 D5 and ADR-035 D4. The manuals are NOT in-repo (verified:
`specs/reference/` holds only the two-anchors paper), so this pass routes through the
manuals' own process — this worksheet is the complete instruction set for it. Audit
Section E closes when this pass lands.**

## 1 · Watermark updates (all three title pages)
- Reference manual 5e and framework manual 6e: "reconciled through ADR-031" →
  **"reconciled through ADR-035"** (033: vocabulary + concordance; 034: Chapter-26 scope;
  035: query surface). This also discharges the audit's finding that neither manual was
  reconciled against ADR-032.
- Frame-QL manual v1: the Coframe-continuation watermark is *kept* (it is true) and gains
  one sentence: "Chapter 2's canonical form is retained as Appendix [X], historical
  lineage; the shipped grammar (ADR-035) is normative."

## 2 · Reference manual Appendix B — the concordance entry
Insert ADR-033 Appendix A verbatim, format-matched to Appendix B's existing entries
(Metric Engine = Combiner + Cache(r) = pre-factoring `ColumnEngine`; contained in the
Columna Engine; map arithmetic Combiner-by-authority).

## 3 · Chapter 26 status marks (ADR-034's partition — the honest-hatching pass)
Tag every construct at its section head. The idiom: **[SHIPPED vX.Y]** · **[SCHEDULED — 
<WP>]** · **[ROADMAP — <note>]**.

| Construct | Mark | Note for the section head |
|---|---|---|
| `MANIFOLD`, `UNIVERSE`, `LEVEL`, `EDGE`, `RELATE`, `MEASURE` (+ inline `M_ANCHOR`/`FAMILY`/`BLOCKED`/`ORDER`), `DERIVED` | **SHIPPED 0.7.8** | the shipped keyword set |
| `DERIVED ... FAMILY { m FERTILE {…} }`, `AT <level>` | **SHIPPED post-0.7.8 (WP-B)** | fertility declarations + resolution anchor; adjudicated per the Certificate kernel (four verdicts; License) |
| `ASSERT` | **SCHEDULED — on-ramp WP** | Certificate-kernel customer: declared, adjudicated at publish (ADR-034 D1) |
| `HIERARCHY` (multi-level chains, verified) | **SCHEDULED — on-ramp WP** | chains desugar to EDGEs; the *verification* (edge functional in the data → verdict) is the new content (ADR-034 D1) |
| `UNIVERSE ... BASIS` (registry/spine/product/events) | **SCHEDULED — on-ramp WP** | typed point-provenance, adjudicated (ADR-034 D1) |
| `ALIAS` | **SCHEDULED — on-ramp+Explorer WP** | communicative only, by definition; renders in the Explorer (ADR-034 D2) |
| `WITHHOLD`, `ACCESS` | **ROADMAP — Pro/enterprise** | requires an authn model Core deliberately lacks (ADR-034 D3) |
| `INHERITS ... VERSION` | **ROADMAP — layered-cache era** | composition waits for the first two-Manifold installation (ADR-034 D4) |
| `COLUMN` long-form fields (`TYPE`/`V_ANCHOR`/`ON`/`M_ANCHOR`/`FAMILY_SET`/`DEFAULT_FAMILY`/`FILL`/`COVERAGE`/`SOURCE`) | **per-field**: mark the fields the shipped `MEASURE` form covers as SHIPPED-via-`MEASURE`; `FILL`/`COVERAGE`/`SOURCE` as **SCHEDULED — on-ramp WP** (they are evidence-grade carriers) | the long form is documentation of the same declarations the short form ships |

Plus one framing paragraph at the chapter head, in the manuals' voice: the chapter
documents the *language*, and each construct states its shipping status; unmarked
constructs do not exist. (The Certificate layer's Part VI gains the same head-note,
pointing at the kernel: License + four verdicts SHIPPED in WP-B; its customers arrive
per the Chapter-26 marks.)

## 4 · Frame-QL manual — the Chapter 2/6 rewrite (ADR-035 D4)
- **Chapter 2 rewritten** to the shipped envelope grammar: `columns @ anchor`; the label
  form `name: expr`; dotted member access (`revenue.sum`, `level.last`); `ON UNIVERSE`
  pins; the inner anchor (`avg(aov@day) @ month`); the clarify vocabulary these produce
  (`co_anchor_ambiguous`, `input_anchor_ambiguous`). State D1's growth rule in the
  chapter's own voice: the grammar grows by ruling, each extension entering with its
  legality doctrine and tests.
- **Chapter 6 examples regenerated by execution**: every worked example is produced by
  running the shipped package at build time (the site's integrity rule, extended); an
  example that stops parsing fails the manual's build. No hand-transcribed wire.
- **The old canonical form** (`FROM/SELECT/AT`, curly-brace sets, the full Chapter-2
  ceremony) moves to a historical appendix labeled *lineage: the Coframe-QL Third
  Edition surface*, with one paragraph noting which of its ideas the shipped grammar
  absorbed (per-subexpression anchors) and which were declined and why (`FROM/SELECT` —
  container ceremony; ADR-035 D2).

## 5 · Acceptance for the pass
Every Chapter-26 construct carries exactly one mark traceable to ADR-034; zero Chapter-6
examples that do not execute against the shipped package; watermarks cite ADR-035;
Appendix B carries the concordance; the Part VI head-note points at the shipped kernel.
The audit's Section E is then closed in full, and the fork registry stands at zero.
