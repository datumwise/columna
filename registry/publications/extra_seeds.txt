# Extra harvest seeds — Zenodo record ids that the registry must cover but that no tracked file
# cited at the moment the registry was created.
#
# The harvester seeds itself from every Zenodo id cited in the repo, then expands each seed's CONCEPT
# to all of its versions. That is closed under versioning but NOT closed under "records we have ruled
# on but never wrote down" — the whole point of Phase 1 is that several current records were absent
# from every surface. These are those ids. Each line: recid, then why it is here.
#
# Once the migration lands, most of these ARE cited (the derived surfaces resolve them from the
# registry, not from literal text), so this file is the record of how they entered — not a permanent
# crutch. It is never pruned: removing a line cannot remove a record, only make the snapshot smaller
# than the registry, which the checker fails on.

21695710   # Open Planner v1.3 — the real current record; the frozen deposit misquotes it as 21632723
22013410   # The Theory of Data v6.1 — current; the site carried v3.1 (/about) and v1.0 (llms.txt)
21958062   # The Theory of Data v6.0 — the superseded member of the canonical supersession example
21959668   # A Primer on the Theory of Data v2.0 — the superseded arm of the Primer triangle
22018549   # A Primer on the Theory of Data v2.2 — current Primer; cited by the canonical Afternoon
21960380   # A Primer on the Theory of Data Applied v1.0 — a SEPARATE work, not a Primer version
21959941   # The Theory of Data Applied v1.0 — a SEPARATE work, not a Primer version
21888464   # The Two Anchors of a Measure v2.0 — current; every page footer carried v1.0
21707446   # The Theory of Data — A Foundations Note v1.1 — current; /about carried v1.0
22046037   # Analytical Governance v1.1 — the registry's first post-Phase-1 supersession event (2026-08-21)

# ── The Evidence pillar (Phase 3B, 2026-08-21) ───────────────────────────────────────────────────
# Nine works, seventeen deposited versions, and not one of them was cited by a single tracked file on
# the morning this was written: the Statistical Bridge corpus existed entirely on Zenodo and entirely
# outside the registry. There is nothing to currentize here and no stale DOI to repair — the failure
# was ABSENCE, which is the one defect a scan of what the repo says can never find. Seeded by ruling
# so the corpus enters before the surface that will cite it, not after.
21979821   # The Statistical Bridge v3.0 — the pillar's constitutional text (concept 21795311)
21980262   # A Primer on the Statistical Bridge v2.0 — the accessible entry (concept 21864433)
21977942   # Where Does Probability Live? v1.0 — the probability-source exposure
21972541   # Certifiable State Under Information Loss v1.0 — warrant conservation, evidence-neutrality
21983508   # Reading Bayesian Workflow Through the Statistical Bridge v1.0
21998564   # Reading Statistical Rethinking Through the Statistical Bridge v2.0 (concept 21863558)
22010143   # The Two Jobs of the Conditioning Bar v1.0 — typed passage, record-undecidability
# Bridge-crossing, membership HELD FOR REVIEW (see specs/publication_corpus_coverage_v0_1.md §3).
# Onboarded as publication identity, NOT as a ruling that they are Evidence-pillar works. `kind` is
# ungoverned for these exactly as it is for the other twenty-eight.
22012515   # Regression Has an Anchor v2.0 — rebuilt across the ToD/Bridge boundary (concept 21783728)
21840854   # Regime Has a Contract v1.0 — regime as a governed object; causal identification

# ── Publication identity closure (Phase 3B.1, 2026-08-21) ────────────────────────────────────────
# The four concepts the creator sweep found and the single-concept model could not hold. Three of the
# four are second concepts or new works; none of them is a new VERSION of anything already seeded,
# which is exactly why concept expansion could never have reached them.
20710593   # Silent Failure Atlas v1.2 — concept 20710592, the Atlas's FIRST attached concept
21893929   # Three Structural Sources v2.0 — concept 21893928; supersedes 21553379 across a concept
21960873   # A Primer on Frame-QL v2.0 — concept 21888997; a work, newly claimed
22026962   # Data Has Its Own Ontology v1.1 — concept 22003682; a work, newly claimed
